{
  "name": "simplyknown-kids",
  "version": "1.0.0",
  "private": true,
  "description": "SimplyKnown Kids — static offline-first PWA. Scripts only: this project has no dependencies and no build step.",
  "scripts": {
    "test": "node --test \"tests/*.test.*\"",
    "stage": "node scripts/stage-site.mjs",
    "deploy:dev1": "npm run stage && node scripts/deploy-dev1.mjs",
    "deploy:prod-preview": "node -e \"console.error('REFUSED: deploy:prod-preview is retired (Deploy & Release Standard PART D8, 2026-09-02) -- it was a second, weaker door to production (clean tree only, no version, no stamp, no Codex triage). The ONE door is: npm run promote (or double-click Promote Kids in Desktop\\\\SimplyKnown Promote\\\\).'); process.exit(1)\"",
    "verify:dev": "node scripts/dev-verify.mjs",
    "promote": "node scripts/promote.mjs"
  }
}
