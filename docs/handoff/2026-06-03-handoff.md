# Handoff — 2026-06-03

**One-liner:** Big session — shipped transparent floating mascots (all 6 animals), 14 verified card fixes + a parent-PIN security fix, a Parent Settings redesign (sidebar/accordion + voice samples), modern Back/Home/Grown-ups chrome + redesigned lock screen, tactile/haptic feedback, a body-parts game that rotates 12 real diverse kids, and **recovered + re-integrated the lost achievements/ribbons system**. All merged to main, **cache v80**, live on kids.simplyknown.co.

> New chat: read this → CLAUDE.md → memory (`achievements_ribbons.md`, `mascot_transparency.md`, `cost_estimate_generation.md`). Stay in auto mode.

---

## Shipped this session (all LIVE on main, cache v80)
- **Mascots:** all 6 (dog/tiger/giraffe/panda/orca/eagle) render green-screen video chroma-keyed to transparent — float, no circle; 4 voices each. Draggable + collision-aware + per-screen re-settle. (memory: `mascot_transparency.md`)
- **Verification + 14 fixes** across all 16 cards × 8 tiers, incl. **closed a parent-PIN bypass**. Reusable sweep: `node tests/sweep/run-sweep.mjs [--only=card] [--tiers=1,8]` (report + screenshots are gitignored).
- **Parent Settings redesign:** sidebar + child-switcher on tablet, accordion on phone, voice options play a sample on tap. ~5,565px → ~1,300px.
- **Nav chrome:** modern flat circular **Back** (coral chevron) + **Home** (teal house, icon-only) via `renderBackBtn()`; faint gear → **"Grown-ups"** pill on the launch screen; redesigned **lock screen** (PIN gate). Activity titles drop below the chrome (`body.vb-chrome .title`).
- **Tactile/haptics:** press-scale + `navigator.vibrate` on buttons/tiles + game success points (reduced-motion safe; no-op on iOS).
- **Body Parts:** rotates **12 real diverse kid figures** (`learning/img/bodies/body-01..12.png`, incl. a wheelchair child) — random on open, switches every 5 correct taps. Source rows in `learning/img/bodies-src/`.
- **Achievements/ribbons — RECOVERED + restored.** Was built ~06-01, stranded on a branch I deleted; recovered from commit 26ca291 (now `7a1d7f2`) and re-wired into every activity + shelf + gallery (`achievements.html`). Earned-ribbon click-through + 16/16 unit tests pass. (memory: `achievements_ribbons.md`)

## Open / pending (nothing uncommitted — see +0 below)
1. **Real-device pass** by Scott — confirm mascots/chrome/haptics/ribbons feel right on his tablet. Needs a one-time site-data clear to get the network-first SW (then auto-updates forever).
2. **Vendor spend:** mascot media generation (Gemini/Imagen + Kling + ElevenLabs + Replicate) ran across the session — check those dashboards for the exact $ (Replicate lip-sync was cheap ~$0.40; Kling idles were the driver). Achievements recovery was free.
3. **Body-parts minor:** body-01/07 faces sit a hair high (tall hair); covered by generous zone radii. Acceptable.
4. **4-voice picker:** woman/man work for all mascots now (all built green); guard falls back to girl if a clip is ever missing.
5. **Stale processes:** a couple of Jun 1–2 python processes from prior sessions/other projects are NOT ours; left alone.

## State / housekeeping
- Branch `claude/reverent-spence-47072a` merged to main. Worktrees consolidated earlier (main + this one; a leftover OneDrive-locked `hopeful-shirley` folder may linger — delete in Explorer/after reboot, git already unreferenced it).
- Local test server (`python -m http.server 8866`) is shut down at session end. Scratch `scripts/_*.mjs` + sweep report are gitignored (kept local).
- **git status is +0** (clean) in the worktree at handoff.
- **Lesson learned (in memory):** before deleting a git branch/worktree, check it for unmerged committed work (`git log <branch> --stat`), not just `git status`. The ribbons loss came from skipping that.
