# Hide & Seek search clues - 2026-09-08 (codex)

## Resume here
1. Scott rejected the location-memory loop: seeing Rabbit over a bush and then pressing Hide gives away the answer. His suggested direction was partial animal clues for younger children. Replaced it with a clue-based search, implemented locally on `kids/child-experience` at application `6a5b7b5`; focused regression `273d010`. Preview: http://localhost:8795/games/peek-a-boo.html . This supersedes the memory-game instructions in the earlier Hide & Seek handoff.
2. Each round introduces Rabbit, Cat or Bear in a neutral card, away from the bushes. Find starts an opaque 700ms hiding cover; only then does the game randomly choose a bush. No location marker, full animal or location text leaks the future answer. The target stays fixed for that round, and can legitimately repeat in a later round.
3. Tiers 1-2 get sustained ears; tiers 3-4 get periodic ear peeks; tiers 5+ get periodic rustling leaves. Every search starts with a clue. A little hint pins a partial peek and names the flower-colored bush accessibly. A wrong guess marks the empty bush and supplies that same useful hint. Only a correct answer reveals the full animal and awards progress once; Play again returns to the neutral introduction.
4. Real 3D clues contain only ears/top-of-head; the normal-motion rustle animates the target foliage, and reduced motion has static gold motion marks. The functional graphics fallback uses partial ears too. The late renderer, visibility changes and page lifecycle retain a coherent round. The legacy activity id, parent settings and existing progress remain intact.
5. Keep the promotion boundary. Main is still `30d28e772aea51d5b19b80fbca46aeb34d05787e`; release version and promote/deploy/verification scripts are unchanged. Cache is `vb-v163`. No push, deploy, main merge, release stamp, paid generation, secret/account read or scheduled task. Earlier ocean artwork/physical-phone acceptance and direct Claude application review remain pending.

## Evidence and corrections
1. Independent baseline check at `1bf2417` failed the new intent requirement: watch had one answer marker and one full animal over a bush, with no neutral introduction. The obsolete location-memory expectations were replaced rather than retained as approval of the rejected design.
2. The independent focused oracle passed **9/9** on the final application through cache v163. It exercises neutral intro, opaque hiding, counts and age clues, hints/retries, single progress award under rapid taps, keyboard, navigation, fallback, late renderer and page lifecycle. The first full run passed273/274; its one failure was a separate old rapid-tap test still waiting for the removed answer marker. Commit`7c676c6` updates that test to use the neutral intro and visible clue; all3 rapid-input tests then passed. Final full regression: **274 passed, 0 failed, 0 skipped**, exit0 (`seek-full-suite-final.log`). App content is`6a5b7b5`, suite at`7c676c6`; the separately verified E2E helper follow-up`5dd018e` changes no shipped application file.
3. **80-screen walkthrough passed, 0 failures**, exit 0 (`seek-walkthrough.log`, ignored), on application `f7809ac` before the final small target/ear positioning corrections. Those corrections receive the focused final review and full regression; they do not change destinations or shell behavior.
4. Final server-stopped offline check installed `vb-v163`, confirmed the game and both modules were cached, stopped its own server, went offline, opened Hide & Seek and completed a real WebGL clue-to-found round (`seek-offline-final.log`). User preview server8795 remains running.
5. Final local staging copied **5,378 files**, with source/staged hashes equal for game page, Games menu, game logic, scene and service worker. Syntax/diff checks pass. Source and screenshots use disposable synthetic profiles; the user's saved profile was preserved.
6. Visual inspection caught ears floating slightly above a bush at the tall comment viewport; the renderer lowered their base and verified all **9 animal/bush combinations at1111x1272**, with visible clues emerging from foliage. Root inspected real 3D Rabbit/Cat/Bear clues at390x844, the graphics fallback, and the actual in-app preview. Scene reports **59 draw calls neutral /69 clue /95 found**; this is not a physical-phone performance benchmark.
7. The small landscape check found one39.94px-high bush target at568x320. Hit areas now expand around their original centers to at least46px inside the stage, while rendered geometry stays unchanged. The hint button also received bottom spacing after an overlap with the grown-up gear was spotted. Final independent review confirmed all three hit areas disjoint, with at least14.8px horizontal gaps, and hint/replay/gear/navigation clear at568x320. Fixed source and visual review found no further material issue.
8. Recorded animal-name introductions and existing correct/retry audio are reused; new search directions remain visible text. No new voice recording or provider charge was made.

## Files and ownership
1. Root: `games/peek-a-boo.html`, `games/index.html`, `js/hide-seek.js`, `sw.js`; integration, offline/packaging/full verification and shared pointers.
2. `ocean_land`: isolated `kids-worlds`, branch `kids/seek-clue-scene`; scene commits `9af30a7` and `af5f0b9`, integrated as `062e5b0` and `317f9ec`. Final artist checkout clean at`af5f0b9`.
3. `ocean_review`: isolated `kids-content`, branch `kids/seek-clue-tests`; test`1ac7c87`, integrated as`273d010`, then independent review of fixed application at`0685ef6`. Follow-up`746f431` updates the two old E2E release helpers to use neutral intro and actual clues; integrated as`5dd018e`. Targeted tier5 phone v2 entry passed6/6 reports; both helper syntax/diff checks passed. A full E2E matrix was not rerun. Reviewer checkout clean at`746f431`; exact claims release with the final handoff.
4. Screenshots/logs and underscore-prefixed helpers are ignored local evidence, not published app assets. Previous Bubble Pop controls and every earlier approved-in-session repair remain.

## Next
1. Scott tries the revised search game in the local preview before any promotion.
2. Continue from the latest user feedback; keep the earlier Kids visual/device and Claude review limits, shared documentation record and Video writer ownership. Do not recreate retired reviews or repeat Notes-only/restart advice.
