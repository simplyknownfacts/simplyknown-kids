# Handoff — 2026-06-02

**One-liner:** Multi-agent sweep of all 16 cards × 8 tiers fixed 14 findings (incl. a parent-PIN bypass); added a draggable, collision-aware mascot that now also **re-settles off any control when you change screens**; shipped a **transparent green-screen chroma-key dog**; and made the service worker **network-first for JS/CSS** so future deploys land without clearing the cache. All merged to main and **live** (cache **v73**).

> New chat: read this → CLAUDE.md → memory (`mascot_transparency.md`, `cost_estimate_generation.md`). Stay in auto mode.

---

## Done this session (all LIVE on main)
- **Verification sweep:** reusable Playwright tool `tests/sweep/run-sweep.mjs` (250 cells, all load clean). Re-run: `node tests/sweep/run-sweep.mjs [--only=card] [--tiers=1,8]`. Screenshots gitignored.
- **14 fixes:** shape-match grid, days/count-along difficulty, hello-colors "Pink Pink Heart", money look-alike coins, finger-paint/stamp-art toolbars, math/abcs/animal-sounds, contrast, age-chip wording. **Security:** closed parent-PIN bypass (locked gate could reset PIN with no knowledge of the old one).
- **Mascot:** drag-to-reposition (tap-preserving), collision-aware drop, floatier look, AND now **re-checks on every screen** — if it's covering a control (e.g. the back button after navigating), it glides to the nearest open spot (`_settleIfCovering`/`_scheduleSettle` in `js/mascot.js`). Note: mascot only runs on index pages (home, games/index, learning/index, art/index, listen/index, videos/index, settings) — not individual canvas activities.
- **Transparent dog:** `CHROMA_MASCOTS` in `js/mascot.js` keys green-screen video → transparent per frame; dog uses `mascots/dog/green/` (image + 9 idle + 40 talking clips). Verified live in-app. Other mascots unchanged.
- **Service worker network-first for HTML+JS+CSS** (`sw.js`): deploys now land on the next online load — no more manual cache-clear. Heavy assets (audio/video/img) stay cache-first.

## Cost (flag)
- Dog build generated **40 talking clips** (4 voices × ~10 phrases) + 9 idles + images — ~2× my original estimate (I misread the voice count). **Check the Replicate dashboard for the real $.** Lesson in memory `cost_estimate_generation.md`.

## Pending / decisions for Scott
1. **Real-device pass** on the live site — confirm the floating dog drags, dodges controls, and animates right on his actual tablet/phone. (To get v73, clear the cache ONE last time; after that the network-first SW makes updates automatic.)
2. **4 voice options:** woman/man dog clips exist but the settings picker only exposes girl/boy (exposing globally would break other mascots that lack those clips). Finish by regenerating other mascots on green w/ 4 voices, or add a per-mascot voice fallback.
3. **Other animals** (tiger/giraffe/panda/orca/eagle) still cream + circle mask — only the dog is transparent so far.
4. **Leftover folder:** `.claude/worktrees/hopeful-shirley-a6888d` is unreferenced by git but physically locked (OneDrive) — delete in File Explorer or after a reboot.
5. **Stale remote branch:** `origin/claude/recursing-gagarin-a837d8` still on GitHub — delete with `git push origin --delete claude/recursing-gagarin-a837d8` if wanted.

## Revert (if needed)
`git revert -m 1 <merge-commit>` then push main. Trivially reversible.

## State / housekeeping
- No background jobs. Worktrees consolidated to **main + reverent-spence** (was 4). Local stale branches deleted.
- Vendor spend this session: dog media generation (Gemini/Imagen + Kling + ElevenLabs + Replicate) — see Replicate dashboard for exact $.
