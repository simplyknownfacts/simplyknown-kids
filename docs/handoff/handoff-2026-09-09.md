# Kids App handoff — 2026-09-09

Context guard hit at 60%. Stopping here per rule 9.2. Next session: read this, verify state, continue.

## What happened this session

1. **Independent code review of Codex's `kids/child-experience` branch** (the full redesign
   handoff Codex sent, `docs/handoff/handoff-2026-09-04.md` from the prior session was the last
   state; Codex's own packet is `Notes\Kids release handoff to Claude — 2026-09-09.md`). Ran the
   whole test suite myself in `.worktrees/kids-child-experience` (branch `kids/child-experience`
   @ `3653a91` at review time, `6c55ae1` after my notes commit) — **280/280 pass, independently
   verified, not just trusting Codex's own number.** Reviewed the highest-risk new code by hand
   (the Three.js 3D world engine, vendored three.js legitimacy, Hide & Seek game logic, PIN
   lockout/gear-hold safety, XSS-escaping conventions). Found no real bugs; found ONE thing worth
   noting: Codex caught and fixed (commit already on that branch, "Codex 0905-1") a real
   fail-open security bug I had introduced in a PRIOR session's exit-PIN lockout code.
2. **Added explanatory comments** to the four densest new files on `kids/child-experience`
   (`js/world-scene.js`, `world-huts.js`, `world-islands.js`, `world-ocean.js` — written much more
   compressed than this codebase's usual style). Comment-only, verified with `node --check` +
   re-ran `tests/world-home.test.mjs` + `world-islands.test.mjs` (30/30, unchanged). Committed on
   that branch: `6c55ae1`.
3. **Deployed `kids/child-experience` to the DEV Cloudflare Pages project** (`kids1.simplyknown.co`
   / `simplyknown-kids1.pages.dev`) so Scott could test on his phone, with his explicit go-ahead.
   Note for next session: `scripts/deploy-dev1.mjs`'s `execFileSync('npx', ...)` throws `ENOENT`
   on this Windows machine (needs `npx.cmd`, not bare `npx`, without `shell:true`) — worked around
   this run by calling the same `wrangler pages deploy` command directly instead of through the
   script. Worth a real fix in `scripts/deploy-dev1.mjs` if this comes up again (not done this
   session — out of scope, didn't want to touch deploy tooling twice in one session beyond what
   was asked).
4. **Consolidated the two "Kids" Desktop promote buttons into one**, per Scott's direct request
   ("I don't like that there are two separate kid deploy bats... consolidate"). Full detail in the
   commit message: `e768dee` on `main` (**not yet pushed** — this is deploy tooling, not app
   content, but it's still a `git push` to `main`, which the project's own Deploy & Release
   Standard treats as a live-production trigger regardless of what changed; needs Scott's
   explicit go before pushing, same as any push to main). New file `scripts/deploy-worker.mjs`,
   `scripts/promote.mjs` now calls it as a step 9 after the site deploys, `promote-kids.bat`'s
   text updated, `promote-kids-worker.bat` relabeled as an emergency-only fallback with its
   Desktop shortcut removed. Tests: `tests/deploy-worker.test.mjs` (new, 3 cases) +
   `tests/promote.test.mjs` (extended/added, 2 cases touching this) — 26/26 in that pair.

## NOT done this session — real bugs Scott just reported, unstarted

Scott tested the `kids/child-experience` dev deploy on his phone and reported, verbatim, right as
the context guard fired (so **none of this was investigated**):

1. **Color In can't see pages** — likely the picture-picker/pager control is missing, invisible,
   or broken on a real phone (worked in the automated browser tests — a real-device-only gap, or
   a viewport/layout issue the tests didn't cover).
2. **Watch can't see any YouTube videos** — channel tiles or the video queue aren't showing
   content at all on a real device/network.
3. **On Watch, "back button within ms"** — Scott's exact words; likely the back control fires
   almost instantly/unintentionally, or reacts within milliseconds of something unintended. Needs
   clarification or direct reproduction.
4. **"rachel youtube goes to main island page not back to video page"** — tapping a video/channel
   (a "Rachel" — likely Ms Rachel, a real configured YouTube channel) navigates to the home 3D
   world instead of back to the video list/player. Sounds like a real navigation bug in
   `videos/index.html` on the `kids/child-experience` branch.

These are all on the `kids/child-experience` branch specifically (the redesign), reached via the
dev preview at kids1.simplyknown.co (behind Cloudflare Access — Scott logs in with
simplyknownfacts@gmail.com, one-time email code). **First thing next session: reproduce and fix
these four, in `.worktrees/kids-child-experience`, tests-first per the Testing Standard, same
discipline as everything else in this repo.**

## State to verify at the start of next session

1. `git status` in both the main checkout and `.worktrees/kids-child-experience` — should both be
   clean.
2. `git log -1` on `main` should show `e768dee` (bat consolidation) as HEAD, NOT pushed yet —
   confirm with Scott before pushing (it's a `git push` to `main`, treated as a live-prod trigger
   by this app's own standard regardless of file content).
3. `git log -1` on `kids/child-experience` should show `6c55ae1` (the notes-only commit) as HEAD.
4. The dev preview at kids1.simplyknown.co is still serving whatever was last deployed there this
   session (`kids/child-experience` @ `3653a91`+notes) — re-deploy if it's been overwritten by
   something else, or once the 4 bugs above are fixed, to get Scott a fresh build to re-test.

## Also still open, carried over from the 2026-09-04 handoff, not touched this session

- 0901-7, 0825-6, 0902-1 from the original Codex ledger sweep (`Notes\Kids — open Codex ledger
  (2026-09-04).md`) — these predate the `kids/child-experience` redesign and may be superseded by
  it (the redesign touches many of the same files); worth checking whether they still apply before
  just re-doing them.
- YouTube Watch privacy/click-shield fix, `AGENTS.md` (this one may already be done — check, a
  later commit on `main` mentions "AGENTS.md is now a pointer").
- `review-fixes` branch — still unmerged, still unverified.

## First things to do in the next session

1. `git status` + `git log -1` in both checkouts (see above) to confirm real state.
2. Ask Scott: OK to push `main` at `e768dee` (the bat consolidation, no app content changed)?
3. Reproduce and fix Scott's 4 reported bugs on `kids/child-experience`, tests-first.
4. Re-deploy to dev, get Scott to re-test on his phone.
5. Once he's happy: full review of the 4 remaining old-ledger items' relevance, then the real
   promote gate + Scott's typed version for actual production.
