# Hide & Seek existing companions - 2026-09-08 (codex)

## Resume here
1. Scott explicitly requested the already-created animated companions instead of the geometric animals in Hide & Seek. Implemented in the isolated `kids/child-experience` checkout; application through `8e4f0c5`, local preview http://localhost:8795/games/peek-a-boo.html . This supersedes only the animal artwork in the earlier search-clues handoff. Keep its neutral introduction, concealed random hiding, age clues, useful hint and one-award round flow.
2. The game cycles Rabbit (`bunny`), Cat (`tabby`) and Panda (`panda`). One reusable keyed canvas/video actor follows the existing companion idle clips, moves from neutral intro to the correct projected bush, clips to ears/upper face for peeks, and shows the full animal when found. The scene no longer builds geometric animals or geometric ear models. The graphics fallback uses the same real companion art.
3. One hidden, muted, playsInline video supplies the keyed canvas. No native video controls or play-arrow surface; no home mascot singleton or saved child companion change. Hiding, page visibility and navigation pause playback. Reduced motion uses the existing master still, or a decoded static video frame if that still fails. Idle-video failure uses the keyed master; failure of both assets supplies a rustling-bush clue. Stale playback completions cannot pause a newly selected animal.
4. Existing recorded Rabbit/Cat speech remains. Panda uses the existing recorded species name Bear and its correct-answer clip; written labels stay Panda. No new voice recording, image generation, provider charge or asset modification. New directions remain written text, as recorded in the prior handoff.
5. Cache `vb-v165` includes the three existing idle clips and three master stills as resilient optional offline assets. The shell update is not made dependent on optional media succeeding. Main is still `30d28e772aea51d5b19b80fbca46aeb34d05787e`; promote/deploy/verification scripts and release version are unchanged. No push, deploy, production merge or release stamp. Scott's preview/device acceptance and earlier direct Claude application review remain pending.

## Evidence
1. Independent baseline negative at `410169d`: intro emoji, no companion actor canvas/video or actual mascot media source. Tests now require decoded existing asset paths, transparent pixels and changing animation pixels, not just an actor label.
2. Focused checks passed: companion media **6/6**, search gameplay **9/9** on final actor/controller fixes. Covers all three species, one actor reused, no raw video surface, concealment, clipping, page lifecycle, reduced motion, missing media and fair progress. Test changes are `7925a70` and `e05f950`; lifecycle follow-up `218442d`.
3. Initial full regression ran **280 cases:273 passed,5 failed,2 cancelled**, exit1 (`seek-companions-full-suite.log`). Investigation found no new gameplay failure: one old rapid-input assertion compared the entire stage HTML before/after pagehide and rejected the intended companion concealment; `218442d` now checks preserved phase/name/hint/one award plus hidden/paused media and no delayed restart. The parent hold passed5/5 separately after one overloaded navigation timeout. The world test exhausted its120s aggregate budget after seven successful viewport drives and other passing behaviors; a separate rerun reproduced only that parent-budget cancellation. `f960511` raises the aggregate budget to180s while keeping all per-operation deadlines and behavior assertions. Final sequential recheck of all affected families (rapid input, parent hold, world) passed **22/22**, zero failed/cancelled/skipped, exit0 (`seek-companions-regression-rechecks.log`). The complete suite was not rerun after those test-only corrections; all initially failing families were rerun, and no application behavior changed after their reviewed fixes.
4. **80-screen walkthrough passed, zero failures**, on final behavior through `7b0e721`; ignored `seek-companions-walkthrough.log`. The final subsequent app change is the service-worker cache version refresh only.
5. Final offline verification installed `vb-v165`, confirmed the game/modules and all six companion assets were cached, stopped its own test server, disabled network, reopened the app, and completed all three real WebGL rounds. Each companion reported decoded video and thousands of opaque and transparent canvas pixels. Evidence: ignored `seek-companions-offline-final.log`. The user preview server8795 remains running.
6. Final stage copied **5,378 files**; hashes match source for game HTML, game controller, scene, shared mascot renderer and service worker. Syntax/diff checks pass. Main/promote/release files were checked unchanged. No generated screenshot or child data is staged.
7. Independent fixed source/visual review clear. Reviewed all three companions in intro/peek/found at390x844,1111x1272 and568x320, across all three bush targets; peeks emerge from the matching bush, found art remains within stage, controls remain clear. Root inspected real WebGL and fallback peeks, plus both refreshed in-app Hide & Seek tabs. Actual preview reports Bunny video, WebGL ready, source video hidden. This is not a physical-phone performance approval.
8. Separate actor edge probes verified a deliberately delayed old play promise cannot stop Panda after switching, static decoded-video fallback with a blocked poster, unavailable state for poster failure plus rejected playback, and no repeated data-state mutations per rendered frame.

## Ownership
1. Root owns game HTML/controller/scene, integration, cache, full/offline/stage verification, and shared note pointers. Application commits `3c6d088`, `96b8c00`, `920f820`, `7b0e721`, `8e4f0c5`.
2. `ocean_land` wrote only `js/mascot.js` in isolated `kids-worlds`, branch `kids/seek-companion-player`, commits `4c20f6e` and `c24eb1d`; clean at the latter. Integrated as `3c6d088` and `7b0e721`.
3. `ocean_review` wrote only tests in isolated `kids-content`, branch `kids/seek-companion-tests`, commits `185b99c`, `115594c` and lifecycle follow-up `441456b`; independent review performed on its fixed clean checkout with matching final behavior. Integrated as `7925a70`, `e05f950` and `218442d`. Root owns the aggregate test-budget correction `f960511`; independent test-harness review clear: aggregate headroom changed, no behavior assertion or operation deadline weakened. This is Codex review, not the still-pending direct Claude application review.
4. Exact Seek companions root/actor/test claims release with this handoff. Prior ocean, body-parts, Bubble Pop, art, parent-setting and other Kids work remains. Video checkout, retired schedules and other projects remain outside this change.

## Next
1. Scott tries the existing animated companions in the refreshed local Hide & Seek preview before any promotion.
2. Continue from the newest user feedback; preserve the earlier shared documentation/Claude continuity, Kids visual/device limits and Video writer ownership. Never repeat Notes-only/restart advice or recreate retired reviews.
