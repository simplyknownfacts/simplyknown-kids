# Handoff — 2026-06-06 — live at v102 — NEXT CHAT: fix the visual-polish items

## ▶ START HERE (do this immediately, don't re-plan)

These are **bug fixes**, not features — **no brainstorming needed.** Work through the **"Items to fix"** list below. For EACH item: **(1) verify it's actually real** (the visual sweep produced false positives — see the "Verified NOT bugs" list — so confirm before touching), **(2) fix the CSS/layout**, **(3) re-verify** (re-screenshot the page locally and look), then once a batch is done, **(4) bump `sw.js` + commit + ask Scott to deploy**. Fix the higher-severity ones first.

## Project basics
- **Repo:** `simplyknownfacts/valiant-breeze` (vanilla JS PWA, GitHub Pages from `main` root). Live: **https://kids.simplyknown.co**. Non-technical owner (Scott) — plain English, end every reply with a numbered todo list, caveman/terse by default (see global CLAUDE.md).
- **Worktree this session:** `…/Kids_App/.claude/worktrees/blissful-dirac-159d53` on branch `claude/blissful-dirac-159d53`. Everything was pushed to `main`, so **`main` HEAD = v102 (commit 2c7f103)** — a fresh chat in the main checkout has the full current state.
- **Deploy:** merge/push to `main` → Pages rebuilds in ~2 min. **Pushing new code to prod requires Scott's in-the-moment "deploy"** (CLAUDE.md 9.9; the auto-mode classifier also blocks it otherwise). After push verify: `curl -s "https://kids.simplyknown.co/sw.js?cb=$(date +%s)" | head -c 24` shows the new `vb-vNN`. To bump: edit `sw.js` line 1 `const CACHE = 'vb-vNN'` + prepend a changelog note.
- **Version is in `sw.js` line 1.** Currently `vb-v102`. Next = `vb-v103`.

## Local test setup (reused by all the sweeps)
- Start a server from the repo/worktree root: `python -m http.server 8791` (background). Test against `BASE_URL=http://127.0.0.1:8791`.
- Playwright: `npm i playwright` (the package isn't committed — repo stays dependency-free — but chromium is already cached at `~/AppData/Local/ms-playwright`, so `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm i playwright` is instant).
- **Visual sweep (use this to verify):** `BASE_URL=http://127.0.0.1:8791 node tests/e2e/v2/visual-shots.mjs` → writes ~75 PNGs to `tests/e2e/v2/visual/`. Read the relevant ones to inspect. (Screenshots are gitignored; the `.mjs` is committed.)
- Other committed tests: `tests/e2e/v2/run-e2e-v2.mjs` (behavioral oracle, `--only=<id>`), `tests/e2e/v2/dead-controls.mjs` (self-validating dead-control sweep — found 0 real dead controls).

## Items to fix (from the visual sweep; triaged — these are the REAL/unconfirmed ones)
Verify each first, then fix. Screenshot name in parens (regenerate via visual-shots.mjs).
1. **Settings header under the decor** — Medium — `parent/settings.html` (phone + tablet). The "Parent Settings" title + the "+ Add child" chip sit under the atmosphere cloud/sun graphics. Fix: give the settings header a higher z-index than the `atmosphere.js` layer (or pad it clear). (shell_settings-phone.png, shell_settings-tablet.png)
2. **Math answer-slot jammed at right edge** — Medium — `learning/math.html`, young layout (tier ≤3) on phone. The `= ▢` slot is hard against the right edge / equation cramped. (learning_math-t2-phone.png)
3. **Finger-paint palette overcrowded/clipped on tablet** — Medium — `art/finger-paint.html`. Bottom palette (8 color dots + Clear + gear) jammed, rightmost clipped at tablet width. Fix: let the palette wrap or scale dots down on wide screens. (art_finger-paint-t6-tablet.png)
4. **Home profile pill cramped/clipped** — Low/Med — `home.html`. The bottom-right profile pill (name · age) is clipped/cramped against the edge (worse on tablet). (shell_home-phone.png, shell_home-tablet.png)
5. **Stamp-art sticker tray ragged on tablet** — Low — `art/stamp-art.html`. Stamp palette wraps raggedly. (art_stamp-art-t6-tablet.png)
6. **Money $5 bill labels hard to read on tablet** — Low — `learning/money.html`. The $5 SVG bill labels are small/low-contrast. (learning_money-t6-tablet.png)
7. **Games-hub ribbons footer clipped** — Low/Med — `games/index.html`. "Games Ribbons" footer + "See all my ribbons" button were clipped at the bottom on phone. This was *partly* the mascot (now removed in v102) — **re-verify** whether it's still clipped before fixing. (shell_games_index-phone.png)
8. **Shape-match young layout at tier 6 phone** — Low/VERIFY — `games/shape-match.html`. Appeared to render the narrow "young" column layout with big dead side-margins on an older-tier phone — confirm whether real (could be by-design). (games_shape-match-t6-phone.png)

## Verified NOT bugs — do NOT "fix" these (false positives the agents flagged)
- **Color-In ‹ › side arrows** — intentional semi-transparent page-flip controls (fixed-position, 4px inset, over the canvas). Working as designed.
- **Peek-a-boo "two identical 🎀 cards"** — those are the **closed curtains** (🎀 = the cover); the animals underneath are distinct (`pickRandom` slices a shuffled unique list). Not a bug.
- **Settings "no left nav on phone"** — the phone layout is an accordion by design; the sidebar is for ≥820px.
- **"Add Child form missing on phone"** — a screenshot-script artifact (it didn't open the form via the real `openAddChild()` button); the form itself works (verified).
- **Ribbon toast overlapping bottom content** — the transient celebration toast caught mid-fade; it auto-dismisses (~1.3s).

## What shipped this session (context; all live at v102)
- v97 Body Parts per-kid zones + image-sliver cleanup + bigger figure; Coloring upload → XDoG line art (clean outlines, not the old threshold speckle).
- v98 Canvas rotation fix (tap-pop/stamp-art/finger-paint/color-splash resize on rotate) — found by a 2-method E2E.
- v99 **Body Parts: ALL 12 kids' face zones re-aligned via automated EYE-DETECTION** (the old hand-tuned offsets were ~4-6% too high — taps on the face missed); **on-screen captions** for prompts + "Yes!/Try again" (works with the volume off, `app.js _showCaption`); **ribbons** less frequent (`REPEAT_SLOW` 50→120) + the award popup is now a small top toast (`.vb-celebrate`) not a full-screen takeover.
- v100 Body Parts bigger figure + no-repeat "bag" (every part asked once before repeats).
- v101 Add-Child form now collects name + birthday + **animal companion (mascot) + voice** (mascot & voice **required**); the 4 dead "Add a child profile first" prompts are now working buttons (`openAddChild()`).
- v102 Mascot removed from the section hubs (Games/Learn/Art/Watch) — it was covering a tile / the ribbons footer on the phone grid; still on home + in activities.

## Key lessons (so the next chat doesn't repeat the misses)
- **Tests asserted wiring/no-crash, not pixels/visuals.** The body-parts harness clicked zones by `data-name` and reported "32/32" while every face was visually misaligned. **Visual correctness needs the screenshot sweep + actually looking.**
- **The visual-sweep agents OVER-FLAG.** ~2 of every 3 spot-checked findings were false positives. **Always verify a flag before fixing it.**
- Memory files updated this session: `body_parts_zones.md`, `coloring_xdog.md` (in the project memory dir).
