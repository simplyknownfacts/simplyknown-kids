# Kids_App — Handoff 2026-05-18

> **If you read nothing else:** Live site `kids.simplyknown.co` is on commit `7e34e19` with SW cache `vb-v51`. **TOP PRIORITY: every activity needs end-to-end testing at each of the 8 age tiers — Scott is hitting real bugs daily in normal use, and the existing Playwright sweeps don't catch age-by-game issues.** See open item #1.

## Active branch / state

1. `main`, no worktrees.
2. Latest commit `7e34e19` is built + live on GH Pages.
3. SW cache `vb-v51` (in `sw.js`).
4. ElevenLabs spend this session: **~$2.10** (3 generation runs).

## What landed this session (high-level, not every commit)

1. **Tiers reorganized.** Same 8 numeric tiers, new labels + age cuts. See `js/tiers.js`. New helper: `tierAgeRange(tier)`.
2. **Per-activity show/hide per kid.** Each entry in `ACTIVITY_FEATURES` (in `js/profiles.js`) now has `section`, `icon`, `file`, and `minTier`. Section index pages render dynamically from this list. Parent Settings has a new **🎯 Activities Available** section — checkbox grid (game × kid). Helpers: `isActivityVisible`, `setActivityVisible`.
3. **Per-feature age recommendations.** Every feature in `ACTIVITY_FEATURES` has a `minTier`. A small teal `<age range>+` chip renders next to each toggle in Parent Settings AND the in-game gear overlay.
4. **Body Parts redrawn.** New cartoon kid SVG in `learning/body-parts.html` with kid-real proportions, hands w/ thumbs, eyes w/ pupils+highlights, hair fringe, etc. No-repeat prompt.
5. **Recovery phrase removed entirely.** PIN gate and Change-PIN card both show *"Forgot? Email simplyknownfacts@gmail.com"*. Stale `vb_recovery` is auto-cleared from localStorage on settings load. Code stripped: overlay markup, `RECOVERY_KEY`, `recoveryMode`, 6 helper fns.
6. **Photo → coloring page rewritten (twice).** First as color-dodge pencil sketch (`69fd34e`); Scott said still too noisy, so rewrote again with downsample→posterize→Sobel→opening pipeline (`7e34e19`). Awaiting feedback.
7. **Count Along emoji dots bumped 70→96 px** so toddler fingers can hit them.
8. **6 new learning activities + Color In:** ABCs, Days, Math Mountain, Spelling Bee, Money, Body Parts, Color In. All wired into Parent Settings + voice manifest. 262 new ElevenLabs clips generated.
9. **Money has presidential silhouettes.** Penny=Lincoln, Nickel=Jefferson, Dime=Roosevelt (faces left), Quarter=Washington (faces left). Edited by user/linter at some point — keep as-is.
10. **Color In is now a 3D-page-turn "book"** with forward/back arrows that flip pages. Also edited by user/linter — keep as-is.
11. **SW cache-busting fixed.** SW precaches with `cache: 'no-store'` + `updateViaCache: 'none'` on registration so each release doesn't strand users on stale JS for 10 min.
12. **Many small bug fixes:** PIN bypass closed, pinch-zoom blocked all platforms, speech auto-cancels on each tap, settings overlay no longer disappears on release, mascot black-circle on Watch/Listen fixed, Color In colors actually paint (CSS specificity bug).

## Open / pending — needs Scott's call

1. **🚩 BIGGEST GAP — end-to-end testing per game per age is missing.** Scott reports many bugs surface every day in real use. Each tile/game needs to be played through at EACH of the 8 tier labels (Sensory → Grade 2+) with a real profile of that age. The Playwright sweeps so far only test one age at a time and only verify "does it render / no JS error" — they don't catch e.g. "drag works wrong at 18 months" or "math choices include the wrong distractors at tier 6". Next chat should:
   1. Build a matrix: 16 activities × 8 tiers = 128 cells.
   2. Create a test profile per tier (set `birthday` to a date that maps to each tier's mid-range).
   3. For each cell, load the activity, play through one full round, snapshot what's visible, check that the round actually completes and feels right for that age.
   4. Treat anything age-inappropriate or broken as a real bug, not a tuning preference.
   5. Make this repeatable so it can run after every release, not as a one-off.
2. **Did the new photo→coloring pipeline fix the noisy-windows complaint?** Re-upload Scott's test photo and tell him. If still noisy, next lever is `WORK_DIM` 400 → 300, or bump `THRESHOLD` 90 → 110.
3. **Tier behavior audit not done.** Tier labels were relabeled and age cuts shifted, but I did NOT audit every `tier <= N` branch inside each activity. So e.g. shape-match drag mode currently fires at `tier >= 4` (old "Drag" = 18-24mo). Under the new mapping, tier 4 = Pre-K (3-4yr) and tier 2 = Explore (1-2yr — when kids can actually drag). Worth a sweep alongside #1 above.
4. **Yoto + YouTube** were not deep-tested this session (no real-device creds / Yoto OAuth). Listen page renders empty-state correctly; YouTube IFrame API auto-skip is shipped but unverified against real 153 errors in the wild.
5. **Body Parts is much better but still cartoon.** If "more realistic" means photo-realistic, would need an external SVG library or licensed asset. Current SVG is hand-authored and kid-friendly cartoon.

## Files modified but not yet committed

1. None. Working tree is clean except for 4 untracked Playwright debug PNGs in repo root (`color-in-now.png`, `color-in-v2.png`, `color-in-v3.png`, `final-color-in.png`). Safe to delete — they're already gitignored.

## Active processes / background jobs

1. Background bash `bhclemo8f` (deploy-wait loop) was running, **exited with status failed (exit 4)** after the system clock change. Already complete — ignore.

## How to resume in next chat

1. Read `Kids_App/CLAUDE.md` (architecture, PIN gate, tier table, hosting) — handoff intentionally doesn't duplicate it.
2. Check live state: `gh api repos/simplyknownfacts/valiant-breeze/pages/builds --jq '.[0].commit'` should still be `7e34e19` or newer.
3. **First question to Scott:** "Did the new windows in the coloring-book photo look right?" — that's the open thread.
4. If touching activity branches, know that `tier` numbers stayed 1-8 but their **meaning shifted** (e.g. tier 4 used to mean 18-24mo, now means Pre-K 3-4yr). Audit before assuming "tier <= 4" still means toddler.
