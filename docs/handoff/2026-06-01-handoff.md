# Handoff — 2026-06-01 (#2)

**One-liner:** Kid-test walkthrough in progress — Card 1 (Tap & Pop) approved by Scott across all ages+settings; Card 2 (Shape Match) mid-test. Storybook redesign live; many tier-behavior, viewport, sync, and design-review fixes shipped (cache now **v71**).

> New chat: **stay in auto mode.** Read this → CLAUDE.md → memory `project_kid_test_1.md` → verify state.

---

## Where we are: the card walkthrough (main program)
Testing all 16 activity cards × 8 age tiers + parent settings. Scott tests on his real device, says "Card N done", we move on.

1. **Card 1 — Tap & Pop: ✅ DONE** (Scott-approved all ages + settings, 2026-06-01).
2. **Card 2 — Shape Match: 🔄 IN PROGRESS** — briefing delivered; Scott was testing. Resume here.
3. Cards 3–16: pending (briefings pre-written, in `project_kid_test_1.md` / earlier audit).

**Resume action:** confirm Card 2 passes all 8 ages + settings → say "Card 2 done" → give Card 3 (Hello Colors) briefing.

## Build / deploy facts
- Live site: `kids.simplyknown.co` (GitHub Pages, deploys from `main`).
- Worktree branch: `claude/recursing-gagarin-a837d8`; merged to `main` after every fix (`--no-ff`). Main HEAD = `60c685f`.
- **SW cache version: v71** (`sw.js` `const CACHE='vb-v71'`). Bump it on EVERY visual/JS change or devices serve stale files.
- **Recurring gotcha:** the service worker serves stale JS/CSS even after a cache bump. Scott must Clear & reset site data per device, OR — standing offer not yet done — rewrite `sw.js` to network-first for JS/CSS so reloads always get fresh files. **Worth doing to end the cache dance.**
- Local test server: `python -m http.server 8866` in the worktree (was killed; restart if needed). Port 8765 is taken by Scott's unrelated TikTok project.

## Tier behavior locked in (Tap & Pop, Scott-tuned)
- Baby(t1): 2-3 slow varied floaters, no auto-pop. Toddler(t2): up to 4, varied speeds.
- t3: wiggle/drift varied. t4: wiggle + some fade partway.
- t5-8: mixed modes (some straight, varied wiggle freq/width, flash in/out, edge-cross), age-scaled speed (younger slower).
- Score counter ALWAYS on (no toggle). Shiny gold +3 bubbles (smaller/faster/twinkle) at every tier.
- Shape Match: drag-and-drop from tier 2 up; count scales 2(toddler)→6(grade2); baby = single big shape, tap→name+bounce. Shows on baby home (minTier 1).
- "young-icons" pattern (bigger icons+spacing for tier≤3) applied to: count-along, hello-colors, animal-sounds, abcs, body-parts.

## Review findings — fixed vs deferred
Ran security / a11y / ux-copy / design reviews. **Fixed (v71):** XSS escaping of cloud data; friendly sign-in errors; PIN lockout message; keyboard focus ring; prefers-reduced-motion; shape-match drop-label.
**Deferred (with reason — do NOT treat as quick fixes):**
1. Plaintext sync token in localStorage — inherent to PWA; XSS fix removed the theft path; rotation is server-side (Cloudflare Worker, not in this repo).
2. Full keyboard nav on `<div onclick>` controls + screen-reader support for canvas games — systemic refactor; touch-first kid app, low ROI.
3. `user-scalable=no` zoom-block — intentional (toddlers pinch-zoom breaks UI), kept despite WCAG.
4. Cosmetic: raw-hex game palettes, unused `--radius` token, parent-settings "SaaS look" — consistent app-wide, skipped.

## Pending decisions for Scott
1. Network-first `sw.js` rewrite to kill the stale-cache problem? (recommended)
2. Cloud sync depends on the **Cloudflare Worker** — if it issues single-session tokens, re-signing-in on device B re-breaks device A. Needs testing on real devices; real fix is server-side. The expired-session prompt + conflict chooser are live (v65+).
3. Pre-generate ElevenLabs voice clips for kid names beyond Noah/Leah (other names get no spoken greeting — robotic TTS is suppressed). Run `scripts/generate-voices.mjs` (needs ElevenLabs key) after adding names to `KID_NAMES` in `js/voice-manifest.js`.
4. impeccable plugin: `npx`-install was blocked by the auto-mode classifier; installed the skill manually instead (copied to `~/.claude/skills/impeccable`). Fine as-is.

## Tools installed this session
- `taste-skill` (design), `impeccable` (design, ~/.claude/skills), `watch`/claude-video (video analysis), `codeburn` v0.9.11 (global npm, run `codeburn`).
- caveman + open-design: declined (caveman risky installer + changes output style).

## State / housekeeping
- Uncommitted in worktree: only untracked `docs/superpowers/` (a plan) + `tests/` (audit screenshots). Not committed by design — safe to leave or gitignore.
- No background jobs running (test server killed).
- Vendor spend: none this session (no paid API calls).
- Photo→coloring windows feedback: still open from the 2026-05-18 handoff, never addressed.
- A stale "running bash 525min" indicator may show — it's cosmetic (process was killed), clears on session restart.
