# Handoff — 2026-06-04

**One-liner:** Ran a full real-click E2E test (208/208 clean on live v80), then did Scott's
6 feedback items: wooden ribbon shelf, gallery scroll fix, removed phone parent-button overlap,
ribbon rebalance (harder tiers + repeatable ×N), parent-settings contrast fix, peek-a-boo wired
into the catalog, and **3 new young-kid games** (Magic Touch, Tap-a-Tune, Surprise Pop). **All
local on the branch, UNCOMMITTED, NOT deployed — live is still v80.** Voice fix is the only thing
left, blocked on Scott's ElevenLabs key.

> New chat: read this → CLAUDE.md → memory (`achievements_ribbons.md`, `e2e_harness.md`,
> `voice_pipeline.md`). Active branch: `claude/relaxed-ellis-4920b4` (worktree). Stay in auto/build mode.

---

## Done this session (all LOCAL + verified, NOT on live)
1. **Full E2E harness** — `tests/e2e/run-e2e.mjs` real-click-drives the live site, 8 isolated
   browsers (one per age), every activity + flow. Final clean run: **208 PASS / 0 WARN / 0 FAIL**.
   App is healthy. (`node tests/e2e/run-e2e.mjs --tiers=1,8 --conc=2`; node_modules + out/ gitignored.)
2. **Wooden shelf** — `.vb-shelf` is now a full-width wood plank with ribbons resting on it
   (`css/achievements.css`); was a narrow glass box.
3. **Gallery scroll** — `achievements.html` now scrolls natively (`<html class="vb-scroll">` +
   override of the global scroll-lock in `css/achievements.css`). Was unscrollable.
4. **Phone overlap** — removed the redundant `.parent-hint` text that sat under the Grown-ups
   pill (`index.html`).
5. **Ribbon rebalance** — `MILESTONE_TIERS` now **50/250/1000/2500/5000/10000** (was 5/10/25/50/75/100);
   added a **repeatable "★ ×N" ribbon** every 25 successes per activity (defs/logic/ribbon/gallery/
   shelf/celebrate). 18/18 unit tests pass. NOTE: thresholds are one shared scale — for slow
   activities (math/spelling) gold=1000 is aspirational; offered to make per-activity if Scott wants.
6. **Parent-settings contrast** — pinned the night palette on `parent/settings.html` so text stays
   readable in every time-of-day theme (afternoon theme flipped `--text` near-black → invisible).
7. **peek-a-boo** wired into `js/profiles.js` catalog (was orphaned/unreachable).
8. **3 new games** (`games/magic-touch.html`, `tap-a-tune.html`, `surprise-pop.html`), each
   tier-scaled, wired into `js/profiles.js` (catalog) + `js/achievement-defs.js` (ribbons).
   Toddler games: 2 → **6**. Verified (load, record, no errors) via `tests/e2e/verify-game.mjs`.

## Pending / blocked
1. **Voice (item 3) — app code PREPPED; generation BLOCKED on Scott's key.** DONE: `js/celebrate.js`
   now speaks the generic clip-backed phrase "You earned a new ribbon!" (added to `js/voice-manifest.js`),
   so awards will use the child's voice once clips exist. REMAINING: add woman/man IDs + a skip-pitch
   guard to `scripts/generate-voices.mjs` (IDs in `generate-mascot.mjs`: woman `21m00Tcm4TlvDq8ikWAM`,
   man `pNInz6obpgDQGcFmaJgB`; grown-up voices must NOT get the +3 kid pitch), then run it. **Needs
   `ELEVENLABS_API_KEY` in a `.env` at project root (absent now) + Scott's go on ~$3.26** (384 × 2 voices).
   Scott chose: he adds the key → I generate + deploy. The 'You earned a new ribbon!' clip also
   regenerates for girl/boy (a few cents).
2. **Deploy** — Scott chose "finish games + voice, then 1 deploy." Games done; deploy waits on
   voice. Deploy = commit branch → bump `sw.js` cache v80→v81 → merge to main → push → Pages
   rebuild (~2-3 min) → one-time site-data clear on device.

## State / housekeeping
- **Everything is uncommitted** on `claude/relaxed-ellis-4920b4`. Nothing pushed. Live = v80.
- **Running job:** local server `python -m http.server 8866` (bg) — kill it (or it's harmless).
- **Vendor spend this session: $0.** Voice generation (~$3.26) not yet run.
- New/edited files (uncommitted): `games/{magic-touch,tap-a-tune,surprise-pop}.html`,
  `js/{profiles,achievement-defs,achievement-logic,ribbon,shelf,progress,celebrate}.js`,
  `parent/settings.html`, `index.html`, `achievements.html`, `css/achievements.css`,
  `tests/achievement-{defs,logic}.test.js`, `tests/e2e/*`, `docs/superpowers/specs/2026-06-03-full-e2e-test-design.md`.
