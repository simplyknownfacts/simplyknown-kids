# Kids true 3D correction — 2026-09-08 (codex)

## Resume here
1. Scott rejected the earlier flat SVG island. This handoff supersedes `handoff-2026-09-08-first-pass-preview.md` for the home design and current verification. His correction is explicit: actual animated 3D, a stationary personal animal in the center that makes sound when tapped, names built into the huts, and no hut taglines or separate greeting button.
2. Reviewable local preview: http://localhost:8795/home.html . The existing server session 61838 serves `.worktrees/kids-child-experience`. `/preview.html` seeds synthetic Explorer only when this localhost origin has no profiles; preserve existing local selections. Scott's visible preview currently uses his selected orca. Leave this server available. Do not repeat failed Codex restart advice or recreate retired reviews.
3. Branch `kids/child-experience`; tested application commit `e60932b03ba6b1a5947e1b8f52f9186ae3d8550a`. The following documentation-only commit records this handoff; read HEAD when resuming. Original production/main is still `30d28e772aea51d5b19b80fbca46aeb34d05787e`; its pre-existing `.publish-real-test/` is untouched.
4. Next is Scott's visual feedback and physical-device play testing. No production/dev push or deploy, merge to main, promote execution, or fabricated dev-verification stamp occurred. Existing promote/dev-verify/staging scripts remain unchanged from main. Keep using the current production gate when a release is eventually authorized; this preview does not satisfy or bypass it.

## Implemented correction
- Live Three.js WebGL scene: volumetric modeled huts, roof geometry, physical signs and posts, glass windows, terrain, paths, trees, lighting and shadows. Flags, hut details, butterflies, clouds, balloon and water animate. No flat image or SVG is used as the home world. Three.js r180 is pinned and self-hosted with its MIT license; no runtime CDN or build step.
- Games, Learn, Art, Watch and Listen are selected by raycasting the actual building meshes, including signs and roofs. DOM buttons provide keyboard and failure-fallback access. Seven tested viewport sizes fit without page scrolling. Wide layout separates the rear/front huts; compact landscape sizes the companion so it cannot cover Learn.
- The selected personal companion stays in the central plaza. Its real button owns taps; embedded media does not intercept them. Tap plays that species' existing sound at audible volume and triggers its existing muted animation. Repeated taps do not overlap a playing reaction; errors, completion and page lifecycle release the lock. Separate hello control, roaming and hut taglines are removed.
- Reduced motion stops decorative rendering and mascot clips. Hidden pages pause. Graphics-context loss exposes usable named huts. A stalled module load exposes those huts after three seconds; a late successful load upgrades back to the 3D scene. Offline Watch and disconnected Listen explain their availability on tap.
- The first-pass content, activity controls, quieter ribbons, protected speech and repeated-input fixes remain in this branch. This correction does not claim every activity has a new mechanic or that every curriculum item has been independently validated.

## Verification on the corrected application
- Full suite: **205 passed, 0 failed, 0 skipped**, 164.6 seconds. Includes seven viewport layouts, physical taps on all navigable huts, 30 repeated hut taps, dragging across huts, keyboard selection, stationary companion during animation, 20 repeated sound taps, reduced motion, lost-context fallback and held-import/late-upgrade behavior.
- Required browser walkthrough: **80 passed, 0 failed**. Includes every registry activity at its selected tier, all ten home tiers, shell sizes, parent gates, ribbon timing, actual mesh navigation, unavailable huts and failed companion media. The deliberately broken URL correctly exited 1 before driving anything.
- Cached operation: all four new runtime modules were present in `vb-v151`. After stopping the isolated origin server, an uncached network probe failed while the 3D home still rendered all five huts; a real Learn tap and Count Along with three answer controls worked. Chromium's emulated `navigator.onLine` remained true in this harness, so server shutdown plus the failing probe is the evidence of network unavailability. Separate browser-drive tests exercise offline UI gating. Physical airplane-mode testing remains outstanding.
- Actual media playback: isolated browser taps on bunny and orca produced `playing` events for rabbit.mp3 and whale.mp3 respectively, with volume 0.7 and muted=false. This is playback evidence, not a claim of listening to every animal/voice variant.
- Local allow-list stage: **5,370 files**, including both Three.js modules, scene and hut code; `preview.html` excluded; staged commit equals `e60932b`. No production gate files changed.
- First over-parallelized verification runs overloaded browser timing checks. The runner now caps file concurrency at two; the award-burst fixture now represents a returning child so an unrelated first-play award cannot race its exact two-award assertion. Final complete run is green; earlier failed logs remain as evidence.

## Review and evidence
The independent Codex review of fixed `95dcba0` in `.worktrees/kids-content` found the stalled-import failure. It re-tested the correction at `e60932b`: timed fallback, physical Listen explanation, late WebGL upgrade, cleared status and stable ready state all passed without page errors. A mistaken ad hoc locator-based test warning was explicitly retracted after checking the committed physical-pointer test. No remaining finding from that bounded review. Builder commits: huts `3407009` and `e7b4d15`, buddy `211a410`; integrated and refined in the root branch.

Direct Claude confirmation of shared documentation was completed earlier. Independent Claude review of the app code remains pending after the previous two bounded attempts timed out; those are not signoffs. No formal G1/G2 or Audit approval is claimed. Physical iPhone/Android/tablet performance, simultaneous fingers, every voice/image variant, advanced tiers and live Watch/Yoto playback remain outside the verified scope. Scott's son's device-specific freeze is not declared fully resolved by desktop automation.

Persistent ignored evidence: `.publish-test-first-pass-evidence/3d-full-suite-final.log`, `3d-drive-final.log`, `3d-drive-negative.log`, `3d-offline-final.log`, `3d-real-audio.log`, and 3D preview PNGs. The walkthrough replaces `docs/verify/shots/` each run. All test profiles are synthetic. The visible browser's existing local profile was preserved. Writer/reviewer claims are released at handoff; Video Bot's separate owner and checkout are untouched.
