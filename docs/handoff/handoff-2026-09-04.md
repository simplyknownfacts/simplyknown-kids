# Kids App handoff — 2026-09-04 (updated, HOLD in effect)

**Stopped per master (claude-bd) HOLD, not a context guard this time.** Scott runs at most 2 PM
chats at once; Cars + Survive hold the slots. Next Kids session resumes from here when master says
go — no new work started, this is a clean stopping point.

## Branch state

`codex-fixes-0904` off `main`, pushed to origin, **never merged** (per master's standing
instruction — this is a live public GitHub Pages deploy on `main`, merging is Scott's click).

Verify with `git log --oneline main..codex-fixes-0904` rather than trusting any list here.

## Progress: 17 of 20 Codex ledger items fixed

Full per-item detail, commit hashes, and test names: `Notes\Kids — open Codex ledger
(2026-09-04).md` under "## Progress" — that file is the source of truth.

**§3 (13 small items): ALL DONE**, plus **§4 (5 of 7 medium done)** this session:
- 0903-2, 0903-3 (dev-verify stamp + prod staging from git HEAD) — done in the prior window
- 0903-5 (`74bafa3`) — promote.mjs's post-approval path now has real test coverage
- 0901-9 (`7f6ee24`) — Yoto mini-player's audio now survives the sleep timer off Listen
- 0825-9 (`346180a`) — exit-PIN dialog gets real lockout (shared with settings), gear-hold fixed to the documented 3s

Also: `docs/CODEX-TRIAGE.md` now carries real FIXED verdicts for 0903-2 and 0903-3 (findings #18,
#19) — the promote gate reads this file and refuses on an untriaged HIGH.

## Remaining (3 items)

From `Notes\Kids — open Codex ledger (2026-09-04).md` §4 and the master-ruled item:
1. **0901-7 MED** — paid-voice caps are count-then-act before 12 ElevenLabs calls (same race
   pattern as an already-fixed 08-25 finding). Fix: insert a reservation row in a D1 batch before
   the first external call; delete it on failure.
2. **0825-6 HIGH** — `/reset` is still a stub; no real password-reset flow (spec exists at
   `docs/superpowers/specs/2026-08-31-password-recovery.md`, not built). This is the biggest
   remaining item — real infra work (Resend send, reset-token table, two endpoints).
3. **0902-1** — sync-worker throttle race (invite check counts-then-inserts; sign-in PBKDF2 runs
   before logging), **already ruled FIXED by master** (size M: reserve-then-check D1 batch,
   per-IP cap regardless of email, indexes + periodic delete) — go straight to building it, no
   more ruling needed. The sync-worker deploy itself stays Scott's manual click; this item is just
   the code fix.

## A real gotcha hit this session, worth knowing before you re-verify

`npm test` (the full `node --test "tests/*.test.*"` run) reliably **stalls partway through** on
this machine when many Playwright-browser test files run concurrently — confirmed NOT a
regression: every file passes clean run individually or in small groups
(`node --test tests/<file>.test.mjs` or a few at a time, ideally with `--test-timeout=25000`).
Verify per-file/small-batch, don't trust a hung full run as a failure signal, and don't burn time
debugging the concurrency issue itself unless it's worth its own ticket.

## Housekeeping still open, not done this session

- `.publish-real-test/` (a gitignored scratch dir from the PRIOR session's staging verification,
  ~5300 files) is still sitting in the repo root. It's git-ignored so it can't get committed by
  accident, but it should be deleted. Bash `rm -rf` and PowerShell `Remove-Item -Recurse -Force`
  were both denied by this session's permission system even after the user explicitly said yes via
  AskUserQuestion — worth trying a different removal approach (or just asking Scott to delete the
  folder by hand) rather than re-attempting the same denied command.

## Also still open, not touched this session (carried over, unchanged)

- The YouTube "Watch" privacy/click-shield fix — master's deadline was "today" as of 2026-09-04
  06:xx; still not worked on, the Codex sweep took priority per explicit master instruction both
  sessions running.
- `AGENTS.md` from the vault template — mentioned as owed, not built.
- Scott's mash-tap glitch bug report — not reproduced/fixed (a nightly robot run also failed to
  reproduce it across 14 activities).
- `review-fixes` branch — stays unmerged until verified; not verified this session either.

## First things to do in the next session

1. `git status` and `git log --oneline main..codex-fixes-0904` to confirm the real current state.
2. Continue with 0901-7, then 0825-6 (the big one), then 0902-1, same discipline (test fails
   first, one commit per item, report in the ledger note + inbox).
3. Circle back to the YouTube Watch fix if master flags it's now overdue.
4. Try again on deleting `.publish-real-test/` if it's still there.
