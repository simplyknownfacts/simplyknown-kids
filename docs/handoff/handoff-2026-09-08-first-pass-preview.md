# Kids first-pass preview — 2026-09-08 (codex)

**Superseded 2026-09-08 (codex):** Scott rejected this flat home design. Continue from [the true 3D correction](handoff-2026-09-08-3d-correction.md); the history below records the rejected first pass.


## Resume here
1. Scott's requested local first-pass revamp is implemented and open for visual review. Preview: http://localhost:8795/preview.html on this computer. Server session 61838 runs from `.worktrees/kids-child-experience`; restart only this loopback preview server if it has stopped. Do not repeat the failed Codex restart/access advice or recreate retired reviews.
2. Branch `kids/child-experience`; tested application revision `ea68a48ba071ab6ffabe6d7b4935cd7c4255479c`. The following documentation-only commit records this handoff; resolve current HEAD rather than guessing it. Original production/main remains `30d28e772aea51d5b19b80fbca46aeb34d05787e` with only pre-existing excluded `.publish-real-test/` scratch. Do not touch that scratch.
3. Next: Scott's visual feedback, physical phone/tablet play testing, then independent Claude review. Two tool-free bounded Claude source-review attempts timed out (240s and 150s); neither returned a review or signoff. The retry packet is in `.publish-test-first-pass-evidence/claude-review-packet.txt`; review checkout `.worktrees/kids-review-first-pass` is detached at `ea68a48`. Review source in a fixed separate checkout, never the live writer tree. The earlier shared-documentation Claude confirmation succeeded; that is separate from this pending code review.
4. No production or dev push/deploy happened. The current promote scripts are unchanged. No dev-verification stamp was fabricated. Main is live GitHub Pages, so merging/pushing it still deploys; the existing cutover/gate questions remain. Scott asked to see this version before production. No formal G1/G2, Audit or Claude signoff is claimed by this preview.

## What changed
An original illustrated responsive island replaces the flat fox image. Games, Learn, Art, Watch and Listen are genuine illustrated buttons with aligned hit areas. The selected child's individual animal roams separately from the background, using its existing animated clips. Reduced motion stops decorative movement. Home fits seven tested phone/tablet/desktop viewports without scrolling. The profile picker, activity hubs, ribbon gallery and shared play controls now use the same visual language; default learning rooms have warm backgrounds and clear dark labels.

The source inventory covers every registered activity plus Watch and Listen. Targeted content changes remove animal-answer spoilers and inaccurate pet habitat answers, teach through wrong-answer feedback, and repair retry/completion behavior. Peek-a-boo is restored with existing age/parent gates. The four art canvases demonstrably accept repeated touch/mouse marks. Finger Paint/Color Splash still overlap; not every activity has a new mechanic or bespoke curriculum illustration in this first pass.

Essential questions resist ordinary feedback and replay-button mashing. Stale callbacks cannot restart speech after leaving. Count Along, Shape Match, Body Parts, Peek-a-boo and Tap-a-Tune have explicit once-only/generation or pointer ownership fixes. Ribbon notices stay small, do not speak or steal focus, batch unlocks, have bounded dwell, and share a 30-second cooldown across pages. The broader drive caught an early age-redirect/audio initialization bug; it was fixed and now has direct regression coverage.

## Verified result
- Full suite on the final application revision: **199 passed, 0 failed, 0 skipped**, 142.7 seconds. This includes release-gate, early age redirect, repeated input, protected/replayed instruction, notice dwell/cooldown, seven home viewports and reduced-motion checks.
- Required browser drive: **80 passed, 0 failed**. It now includes restored Peek-a-boo and tests actual house navigation, disconnected Listen, offline Watch and media fallback behavior. Old panorama-hotspot assertions were replaced with checks of the new user behavior.
- Offline Chromium: new home, home-to-Learn navigation and Count Along worked after initial online load. New shell assets were present in cache `vb-v149`; Count displayed three live answer controls offline.
- Independent art runtime evidence: two gestures per canvas caused distinct whole-buffer pixel changes in Finger Paint, Color Splash, Color In and Stamp Art, with visible screenshots and no page errors. See `docs/child-experience-review-2026-09.md` for exact limits and counts.
- Local stage: **5,364 tracked publish files** built from the final application revision into `.publish-test-first-pass-stage/`. New world assets were included; local `preview.html` was excluded; stage `version.json` names the tested commit. App version **1.1.0**. `scripts/promote.mjs`, `scripts/dev-verify.mjs`, both staging implementations and `promote-kids.bat` are unchanged from baseline.
- Evidence: `.publish-test-first-pass-evidence/` contains final suite, drive, offline and age-gate logs plus the Claude retry packet/timeout note. `docs/verify/shots/` holds current walkthrough and phone/desktop preview images. All evidence and stage directories are ignored, and no customer data was used. The standard driver clears its shots directory before each run; keep persistent logs outside it.

## Boundaries and remaining validation
The synthetic local Explorer/bunny profile is created only when localhost has no existing profiles; existing family profiles are preserved. Physical iPhone/Android/tablet testing, simultaneous fingers, actual hearing of all voice variants, advanced tier modes, every Body Parts image, device motion and live Watch/Yoto playback remain unverified. Desktop automated evidence does not establish that Scott's son's device-specific freeze is fully resolved. Existing animal video assets remain a mixed visual style; this is the first reviewable implementation.

Three focused builder subagents used separate claimed worktrees: worlds `8a8d4ac`, content `869fd0a8`, resilience `3e09b7c` plus `d4e9dd4`. Their commits are integrated. All worktree writer claims are released when the preview handoff is delivered. Keep source work on this branch until Scott reviews it; Video Bot has a separate active owner and was not touched.
