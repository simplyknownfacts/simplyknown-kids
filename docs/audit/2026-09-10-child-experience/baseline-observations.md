# Baseline observations — 2026-09-10 (codex)

1. Baseline `c96cdbc`, visible in-app Chromium, local port 8798, synthetic Explorer birthday 2021-01-01 (T6 on audit date). Viewports verified 1280×900 and 390×844. A screenshot taken immediately after resize showed an intermediate compositor frame; the following screenshot/DOM viewport was correct. Do not report that transient tool capture as an app defect.
2. **Coverage remains incomplete:** 22 activities inventoried; 2 activities partially exercised in 4 T6/device combinations; 5 shared T6/device combinations partially exercised. Zero combinations have completed the full requested checklist. `coverage.json` tracks 440 activity combinations plus 220 shared combinations, each with 25 check dimensions. Of the 440 activity rows, 392 are visible by default; 48 are default-hidden and still require visibility/override/direct-route checks. `BLK` with `NOT_RUN` or `PARTIAL` is explicitly unfinished work, not a discovered product blocker. No activity-level PASS yet.

## Observed interactions

| Route / device | Actual actions and result | Still unproven |
|---|---|---|
| `/home.html`, T6 desktop | Existing localhost preview seeds Explorer; home draws five islands and bunny. Click Games → `/games/index.html`. Listen is aria-disabled without Yoto connection. | Other ages, keyboard, phone home, all other controls, offline, long play |
| `/games/index.html`, T6 desktop + phone | All eight registered game buttons appear; desktop Bubble Pop launch and phone Hide & Seek launch work. Phone return from Bubble Pop succeeds. | Ribbons interactions, home button, all remaining games, every other tier |
| `/games/tap-pop.html`, T6 desktop + phone | Canvas animates colored bubbles; instruction `Pop only Orange!`, score 0, back/home/Again/settings visible. One desktop click and three repeated phone clicks attempted at last-observed bubble coordinates; score stayed 0. Moving targets may have moved before clicks. Back returns to Games; First Bubble Pop ribbon exists (source calls `firstPlay` on entry). | No confirmed scoring defect. Correct target hit, wrong-color hit, successful progression and duplicate reward protection are NOT proven. No keyboard/offline/restart/long-play proof |
| `/games/peek-a-boo.html`, T6 phone | Find Rabbit → No peeking → three bushes. Pink bush clicked twice → `Nobody there. Look for little ears at the blue flower bush.` Pink becomes disabled. Blue clicked twice → `You found Rabbit!`; other guesses disabled. Caption and rabbit reveal visible. | Reward count was not independently inspected; two taps alone do not prove all rapid-input cases; hints, reload, offline, all other tiers |
| `/games/peek-a-boo.html`, T6 desktop | Resize completed round to desktop; Play again → new Cat introduction and Find Cat button. No clipping visible in sampled screenshots. | Full desktop round, keyboard, every other negative case |
| `/listen/index.html`, T6 desktop | Direct local entry renders Yoto cards / Not connected to Yoto / Parent Settings → Yoto, sleep timer, Tap-a-Tune link and Coming soon content. No Yoto account/state was supplied. | No library/API call or account flow attempted. Sleep timer and adjacent audio not yet exercised |
| `/parent/settings.html`, T6 desktop | Direct entry shows first-time PIN setup, confirming this synthetic origin has no configured parent PIN. | Parent Yoto panel was inventoried in source, NOT visually opened; no real or synthetic PIN was entered through the browser |

## Findings classification

1. **Confirmed product requirement YOTO-REMOVE:** Scott requires every Yoto surface absent. Baseline visible evidence confirms unsupported connection prompts are still offered. This is authorized implementation, not a taste question. See `yoto-inventory.md`.
2. **Confirmed behavioral defects:** none yet reproduced to sufficient certainty. This is not a clean audit verdict; coverage is mostly unrun.
3. **Design-quality findings:** none finalized. Two game screenshots are insufficient to grade the whole product or age fit.
4. **Subjective taste calls:** none finalized.
5. **Blocked/unfinished checks:** initial test environment lacked Playwright; standard local server does not apply Cloudflare CSP; physical devices/sensors, heard audio, offline and external-service failures remain untested. Chrome creation timed out once; in-app Chromium worked. No user-facing browser errors/warnings returned by the final local-tab log query (only that query's available buffer, not comprehensive console proof).

## Evidence and health check

1. Ignored screenshot directory: `docs/verify/shots/audit-20260910/`. Files: `home-desktop.png`, `games-desktop.png`, `games-phone.png`, `bubble-phone.png`, `seek-phone-wrong.png`, `seek-phone-found.png`, `seek-desktop-restart.png`, `yoto-before-listen-desktop.png`, `parent-before-pin-gate.png`.
2. Initial `npm test`: 199 tests reported; 155 pass, 17 fail, 27 skipped; missing Playwright caused setup failures. This is not app regression evidence. Full output `health-initial.txt` in ignored evidence directory. Created ignored worktree `node_modules` junction to existing canonical Kids dependency directory; installed Playwright there reports 1.62.1. No package mutation/install; tests not rerun after junction yet.
3. Added `tests/yoto-retired.test.mjs`: four requirement guards, **0 pass / 4 fail**, exit 1 against unchanged app baseline. Compact red result `yoto-red.txt`; raw output `yoto-red-full.txt` in ignored evidence directory. These guards are expected red until implementation. They are not sufficient runtime proof by themselves.
4. No app implementation, production access, deploy, promotion, merge, push, gate edit, external API call, paid service or real family data used in this task.

## Core E2E at `df25aec`

1. The accepted Yoto-withdrawal app commit `df25aec3ba72b33e698d16a40fac7004e8f2171e` was served locally at `http://localhost:8798`. The test-tool and audit-document edits were uncommitted and do not change shipped behavior. Synthetic profiles only; no provider, production, credential, PII or paid call.
2. Repaired the existing real-click harness before trusting it: added named desktop 1280×900 and touch-phone 390×844 runs with isolated outputs; selected the visible settings control; reconciled Bubble Pop, ABCs and every feature toggle with the registry; retained warning error samples; made older-tier Bubble Pop tap a visible target-colour canvas pixel instead of guessing; and made Spelling Bee wait for each filled slot. Invalid viewport input exits non-zero. One-tier control runs passed 32/32 on each viewport.
3. Full T1–T10 results: desktop 318 PASS / 2 WARN / 0 FAIL of 320; phone 319 PASS / 1 WARN / 0 FAIL of 320. The warnings had zero page, console or network errors: desktop T8 and T10 Bubble Pop did not score through the old randomized coordinate probe, and phone T9 Spelling Bee did not finish through the old timing probe.
4. Focused reruns resolved all three tester uncertainties. Desktop T8 Bubble Pop scored; after the deterministic probe repair, desktop T10 Bubble Pop scored by tapping the displayed target colour and its complete tier run passed 32/32. After the slot-wait repair, phone T9 Spelling Bee completed and its tier run passed 32/32. No product defect was reproduced from these warnings.
5. Conservative ledger import: all 440 activity combinations are now PARTIAL, never complete. A passing direct hidden-tier load records only `launch`; a passing interactive cell records `launch` and the interactions/progression actually established by its recipe. The six exercised shared routes add 120 tier/device rows; combined with prior evidence, 562/660 rows are PARTIAL, 98 shared rows remain NOT_RUN, and zero rows have a final verdict. Totals are 1,305 PASS dimensions, 15,195 BLK, 0 FAIL and 0 NA.
6. These runs establish bounded launch, one successful input/outcome path, expected section counts, add/delete child, PIN unlock, available feature toggles, voice selection, activity hide/show and ribbon-gallery loading across both viewports and all tiers. They do not establish restart/back/home, every wrong answer, boundary/drag/keyboard abuse, navigation/reload/resize recovery, failed media/network, offline, corrupt state, timer/audio interruption, long repeated play or final visual quality for every combination. Those remain BLK, not passed by implication.
7. Regenerable JSON reports and screenshots remain ignored under `tests/e2e/out/`; `coverage.json` plus this note are the durable evidence. The import helper refuses incomplete reports or any hard failure and skips unresolved warnings.

## Games resilience at `d6687a3`

1. The accepted app baseline is `d6687a38b2a466e13de00f9553efeb23e9f84d8f`; later commits in this branch change test/audit evidence only. The self-owned local server is checked before every row. Synthetic profiles only; Memory Match T1 uses the supported parent visibility override so its normally hidden tier is exercised rather than silently skipped.
2. Final run scope: all 8 Games × T1-T10 × desktop 1280×900 and touch-phone 390×844 = 160 rows. Each row probes visible instructions, Back/Home, rapid and boundary input, unrelated keys, applicable outside-drag/wrong-answer behavior, resize/orientation, mid-round reload, navigation during active animation, repeated exit/re-entry, malformed synthetic state, applicable age/storage boundaries, observed media/audio failure paths, controlled offline reload and viewport bounds. Timer expiry is explicitly NA because no game exposes a countdown-expiry mechanic. Long repeated play and full visual-quality judgement remain BLK; geometry alone does not complete either.
3. **Confirmed P2 child-experience defect — Bubble Pop T1-T4 has no visible instruction on phone or desktop (8 failed cells).** Reproduction: create/select a T1, T2, T3 or T4 child; open Games → Bubble Pop. Expected: an immediate age-appropriate cue such as “Tap the bubbles!” that explains the play action. Actual: only Back, Home, score and moving bubbles appear; no title, hint or instruction is rendered. T5-T10 correctly render the target-colour banner and pass the same check. Source cause: `games/tap-pop.html` only displays and speaks an instruction inside the `tier >= 5` target-colour branch. Proposed bounded repair for separate authorization: add a compact visible toddler cue plus the existing replay-instruction affordance for T1-T4, without changing scoring/motion, then rerun these 8 cells and the menu/rapid-input suites. No product fix was made in this audit batch.
4. Final guarded report: 160 rows, 2,115 PASS checks, 8 FAIL, 718 explicit mechanic-level NA and 359 BLK, with no runner/runtime or other product failures. The importer ignores 160 internal layout-bound checks, records 1,955 evidence PASS plus the 8 expected failures and applicable NA results, and leaves unforced or judgement-heavy checks BLK. The full ledger is now 3,238 PASS / 8 FAIL / 12,538 BLK / 716 NA across 16,500 dimensions; 8 rows have verdict FAIL and 652 remain BLK. Failure and comparison screenshots remain ignored under `tests/e2e/out/games-resilience/`; representative T1 and T5 phone captures were visually inspected and match the DOM result.
5. Recovery incident: the original claimed `e2bb` worktree disappeared during an uncommitted runner pass, and its old preview began returning `not found` for every route. That 160-row/270-failure output is invalid environment evidence and was not imported. The branch remained intact at `d6687a3`; work resumed in clean claimed `f502`, and the rebuilt runner was committed in small checkpoints before rerun.
6. Scott's non-production Cloudflare company-login exception was received and recorded in the Kids inbox. This batch used only the self-owned localhost server, so no identity-provider login, credential, provider/account change, remote data mutation or production access occurred.
7. Verification after the final import: full project suite 287/287 passed with zero fail/skip; runner and importer syntax checks plus `git diff --check` passed. The standalone resilience runner intentionally exits non-zero while the 8 confirmed product failures remain.
