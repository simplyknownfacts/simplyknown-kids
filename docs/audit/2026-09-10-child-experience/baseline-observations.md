# Baseline observations — 2026-09-10 (codex)

1. Baseline `c96cdbc`, visible in-app Chromium, local port 8798, synthetic Explorer birthday 2021-01-01 (T6 on audit date). Viewports verified 1280×900 and 390×844. A screenshot taken immediately after resize showed an intermediate compositor frame; the following screenshot/DOM viewport was correct. Do not report that transient tool capture as an app defect.
2. **Coverage remains incomplete:** 22 activities inventoried; 2 activities partially exercised in 4 T6/device combinations; 5 shared T6/device combinations partially exercised. Zero combinations have completed the full requested checklist. `coverage.json` tracks 440 activity combinations plus 220 shared combinations, each with 25 check dimensions. Of the 440 activity rows, 392 are visible by default; 48 are default-hidden and still require visibility/override/direct-route checks. `BLK` with `NOT_RUN` or `PARTIAL` is explicitly unfinished work, not a discovered product blocker. No activity-level PASS yet.

## Observed interactions

| Route / device | Actual actions and result | Still unproven |
|---|---|---|
| `/home.html`, T6 desktop | Existing localhost preview seeds Explorer; home draws five islands and bunny. Click Games → `/games/index.html`. Listen is aria-disabled without Yoto connection. | Other ages, keyboard, phone home, all other controls, offline, long play |
| `/games/index.html`, T6 desktop + phone | All eight registered game buttons appear; desktop Bubble Pop launch and phone Hide & Seek launch work. Phone return from Bubble Pop succeeds. | Ribbons interactions, home button, all remaining games, every other tier |
| `/games/tap-pop.html`, T6 desktop + phone | Canvas animates colored bubbles; instruction `Pop only Orange!`, score 0, back/home/Again/settings visible. One desktop click and three repeated phone clicks attempted at last-observed bubble coordinates; score stayed 0. Moving targets may have moved before clicks. Back returns to Games; First Bubble Pop ribbon exists (source calls `firstPlay` on entry). | No confirmed scoring defect. Correct target hit, wrong-color hit, successful progression and duplicate reward protection are NOT proven. No keyboard/offline/restart/long-play proof |
| `/games/peek-a-boo.html`, T6 phone | Find Rabbit → No peeking → three bushes. Pink bush clicked twice → `Nobody there. Look for little ears at the blue flower bush.` Pink becomes disabled. Blue clicked twice → `You found Rabbit!`; other guesses disabled. Caption and rabbit reveal visible. | Reward count was not independently inspected; two taps alone do not prove all rapid-input cases; hints, reload, offline, all other tiers |
| `/games/peek-a-boo.html`, T6 desktop | Resize completed round to desktop; Play again → new Cat introduction and Find Cat button. No clipping visible in sampled screenshots. | Full desktop round, keyboard, every other negative case |
| `/listen/index.html`, T6 desktop | Direct local entry renders Yoto cards / Not connected to Yoto / Parent Settings → Yoto, sleep timer, Tap-a-Tune link and Coming soon content. No Yoto account/state was supplied. | No library/API call or account flow attempted. Sleep timer and adjacent audio not yet exercised |
| `/parent/settings.html`, T6 desktop | Direct entry shows first-time PIN setup, confirming this synthetic origin has no configured parent PIN. | Parent Yoto panel was inventoried in source, NOT visually opened; no real or synthetic PIN was entered through the browser |

## Findings classification

1. **Confirmed product requirement YOTO-REMOVE:** Scott requires every Yoto surface absent. Baseline visible evidence confirms unsupported connection prompts are still offered. This is authorized implementation, not a taste question. See `yoto-inventory.md`.
2. **Confirmed behavioral defects:** none yet reproduced to sufficient certainty. This is not a clean audit verdict; coverage is mostly unrun.
3. **Design-quality findings:** none finalized. Two game screenshots are insufficient to grade the whole product or age fit.
4. **Subjective taste calls:** none finalized.
5. **Blocked/unfinished checks:** initial test environment lacked Playwright; standard local server does not apply Cloudflare CSP; physical devices/sensors, heard audio, offline and external-service failures remain untested. Chrome creation timed out once; in-app Chromium worked. No user-facing browser errors/warnings returned by the final local-tab log query (only that query's available buffer, not comprehensive console proof).

## Evidence and health check

1. Ignored screenshot directory: `docs/verify/shots/audit-20260910/`. Files: `home-desktop.png`, `games-desktop.png`, `games-phone.png`, `bubble-phone.png`, `seek-phone-wrong.png`, `seek-phone-found.png`, `seek-desktop-restart.png`, `yoto-before-listen-desktop.png`, `parent-before-pin-gate.png`.
2. Initial `npm test`: 199 tests reported; 155 pass, 17 fail, 27 skipped; missing Playwright caused setup failures. This is not app regression evidence. Full output `health-initial.txt` in ignored evidence directory. Created ignored worktree `node_modules` junction to existing canonical Kids dependency directory; installed Playwright there reports 1.62.1. No package mutation/install; tests not rerun after junction yet.
3. Added `tests/yoto-retired.test.mjs`: four requirement guards, **0 pass / 4 fail**, exit 1 against unchanged app baseline. Compact red result `yoto-red.txt`; raw output `yoto-red-full.txt` in ignored evidence directory. These guards are expected red until implementation. They are not sufficient runtime proof by themselves.
4. No app implementation, production access, deploy, promotion, merge, push, gate edit, external API call, paid service or real family data used in this task.

## Core E2E at `df25aec`

1. The accepted Yoto-withdrawal app commit `df25aec3ba72b33e698d16a40fac7004e8f2171e` was served locally at `http://localhost:8798`. The test-tool and audit-document edits were uncommitted and do not change shipped behavior. Synthetic profiles only; no provider, production, credential, PII or paid call.
2. Repaired the existing real-click harness before trusting it: added named desktop 1280×900 and touch-phone 390×844 runs with isolated outputs; selected the visible settings control; reconciled Bubble Pop, ABCs and every feature toggle with the registry; retained warning error samples; made older-tier Bubble Pop tap a visible target-colour canvas pixel instead of guessing; and made Spelling Bee wait for each filled slot. Invalid viewport input exits non-zero. One-tier control runs passed 32/32 on each viewport.
3. Full T1–T10 results: desktop 318 PASS / 2 WARN / 0 FAIL of 320; phone 319 PASS / 1 WARN / 0 FAIL of 320. The warnings had zero page, console or network errors: desktop T8 and T10 Bubble Pop did not score through the old randomized coordinate probe, and phone T9 Spelling Bee did not finish through the old timing probe.
4. Focused reruns resolved all three tester uncertainties. Desktop T8 Bubble Pop scored; after the deterministic probe repair, desktop T10 Bubble Pop scored by tapping the displayed target colour and its complete tier run passed 32/32. After the slot-wait repair, phone T9 Spelling Bee completed and its tier run passed 32/32. No product defect was reproduced from these warnings.
5. Conservative ledger import: all 440 activity combinations are now PARTIAL, never complete. A passing direct hidden-tier load records only `launch`; a passing interactive cell records `launch` and the interactions/progression actually established by its recipe. The six exercised shared routes add 120 tier/device rows; combined with prior evidence, 562/660 rows are PARTIAL, 98 shared rows remain NOT_RUN, and zero rows have a final verdict. Totals are 1,305 PASS dimensions, 15,195 BLK, 0 FAIL and 0 NA.
6. These runs establish bounded launch, one successful input/outcome path, expected section counts, add/delete child, PIN unlock, available feature toggles, voice selection, activity hide/show and ribbon-gallery loading across both viewports and all tiers. They do not establish restart/back/home, every wrong answer, boundary/drag/keyboard abuse, navigation/reload/resize recovery, failed media/network, offline, corrupt state, timer/audio interruption, long repeated play or final visual quality for every combination. Those remain BLK, not passed by implication.
7. Regenerable JSON reports and screenshots remain ignored under `tests/e2e/out/`; `coverage.json` plus this note are the durable evidence. The import helper refuses incomplete reports or any hard failure and skips unresolved warnings.

## Games resilience at `d6687a3`

1. The accepted app baseline is `d6687a38b2a466e13de00f9553efeb23e9f84d8f`; later commits in this branch change test/audit evidence only. The self-owned local server is checked before every row. Synthetic profiles only; Memory Match T1 uses the supported parent visibility override so its normally hidden tier is exercised rather than silently skipped.
2. Final run scope: all 8 Games × T1-T10 × desktop 1280×900 and touch-phone 390×844 = 160 rows. Each row probes visible instructions, Back/Home, rapid and boundary input, unrelated keys, applicable outside-drag/wrong-answer behavior, resize/orientation, mid-round reload, navigation during active animation, repeated exit/re-entry, malformed synthetic state, applicable age/storage boundaries, observed media/audio failure paths, controlled offline reload and viewport bounds. Timer expiry is explicitly NA because no game exposes a countdown-expiry mechanic. Long repeated play and full visual-quality judgement remain BLK; geometry alone does not complete either.
3. **Confirmed P2 child-experience defect — Bubble Pop T1-T4 has no visible instruction on phone or desktop (8 failed cells).** Reproduction: create/select a T1, T2, T3 or T4 child; open Games → Bubble Pop. Expected: an immediate age-appropriate cue such as “Tap the bubbles!” that explains the play action. Actual: only Back, Home, score and moving bubbles appear; no title, hint or instruction is rendered. T5-T10 correctly render the target-colour banner and pass the same check. Source cause: `games/tap-pop.html` only displays and speaks an instruction inside the `tier >= 5` target-colour branch. Proposed bounded repair for separate authorization: add a compact visible toddler cue plus the existing replay-instruction affordance for T1-T4, without changing scoring/motion, then rerun these 8 cells and the menu/rapid-input suites. No product fix was made in this audit batch.
4. Final guarded report: 160 rows, 2,115 PASS checks, 8 FAIL, 718 explicit mechanic-level NA and 359 BLK, with no runner/runtime or other product failures. The importer ignores 160 internal layout-bound checks, records 1,955 evidence PASS plus the 8 expected failures and applicable NA results, and leaves unforced or judgement-heavy checks BLK. The full ledger is now 3,238 PASS / 8 FAIL / 12,538 BLK / 716 NA across 16,500 dimensions; 8 rows have verdict FAIL and 652 remain BLK. Failure and comparison screenshots remain ignored under `tests/e2e/out/games-resilience/`; representative T1 and T5 phone captures were visually inspected and match the DOM result.
5. Recovery incident: the original claimed `e2bb` worktree disappeared during an uncommitted runner pass, and its old preview began returning `not found` for every route. That 160-row/270-failure output is invalid environment evidence and was not imported. The branch remained intact at `d6687a3`; work resumed in clean claimed `f502`, and the rebuilt runner was committed in small checkpoints before rerun.
6. Scott's non-production Cloudflare company-login exception was received and recorded in the Kids inbox. This batch used only the self-owned localhost server, so no identity-provider login, credential, provider/account change, remote data mutation or production access occurred.
7. Verification after the final import: full project suite 287/287 passed with zero fail/skip; runner and importer syntax checks plus `git diff --check` passed. The standalone resilience runner intentionally exits non-zero while the 8 confirmed product failures remain.

## Learning resilience at `f782603`

1. The shipped-app baseline remains `d6687a38b2a466e13de00f9553efeb23e9f84d8f`; commits through runner `f782603` change only test/audit evidence. A self-owned free-port localhost server was identity-checked as Kids before every row. Synthetic profiles used deterministic valid randomness and the product's default age modes; no real family state, provider, credential, production or paid call was used.
2. Final scope: all 10 Learning routes × T1-T10 × desktop 1280×900 and touch-phone 390×844 = 200 rows. Each row checks visible guidance, Back/Home, rapid and control-boundary input, unrelated keys, applicable wrong-answer rejection plus correct retry, resize/orientation, mid-round reload, navigation during activity, repeated exit/re-entry, malformed synthetic feature/tier state, media rejection, controlled offline reload and viewport bounds. Dedicated restart, reward correctness, long soak and full visual judgement remain BLK; score, drag and countdown expiry are explicit NA because Learning exposes none of those mechanics.
3. **Confirmed P2 child-experience defect — Animal Sounds T1-T4 has no title, visible instruction, protected spoken instruction or replay control on phone or desktop (8 failed canonical cells).** Reproduction: create/select a T1-T4 child; open Learning → Animal Sounds. Expected: immediate guidance such as “Tap an animal to hear its sound.” Actual: navigation, one or more floating named animals and settings appear with no cue. The garden branch returns before rendering the quiz screen that owns `#instruction`; its ordinary `speak()` call occurs only after a child already knows to tap an animal. No fix was made.
4. **Confirmed P2 child-experience defect — Spelling Bee T6-T10 clips phone letter banks outside the 390px viewport with no horizontal scroll (5 failed visual-quality cells); T10 can hide a required letter (input and wrong-recovery also fail).** Deterministic T10 rendered required `U` at `x=-64..-8` while the fixed screen reported `scrollWidth=390` and `scrollHeight=clientHeight=844`; `scrollIntoView()` could not move it, so GUITAR could not be completed. T6-T9 sampled clipped decoys and still completed, but the choices were visibly incomplete. No fix was made.
5. Guarded final report: 200 rows, 3,036 PASS checks, 20 expected FAIL checks, 950 explicit mechanic-level NA and 795 BLK, with zero runner/runtime or unexpected product failures. Five internal `layout_bounds` failures validate the Spelling geometry but are not ledger dimensions. The importer recorded 2,841 evidence PASS, 15 canonical FAIL and 950 applicable NA results; all other dimensions stayed BLK.
6. Current canonical ledger: **5,878 PASS / 23 FAIL / 8,933 BLK / 1,666 NA** across 16,500 dimensions. Twenty-one rows now have verdict FAIL and 639 remain BLK; 562 rows are PARTIAL and 98 shared rows remain NOT_RUN. Unexecuted work is still distinct from reproduced product failure or an environment/access block.
7. Failure captures remain ignored under `tests/e2e/out/learning-resilience/failures/`. Representative Animal Sounds T4 desktop and Spelling T10 phone captures were inspected; they match the DOM evidence. Automated media success was not treated as human-heard audio proof, and the media-rejection checks prove recovery only. Physical devices, full visual judgement, long repeated play and audible voice quality remain unproven.
8. The standalone runner intentionally exits non-zero while the exact 20 report-level failures remain. `record-learning-resilience.mjs` refuses an incomplete report, a wrong baseline, duplicate rows, any unexpected failure or any missing expected reproduction before touching `coverage.json`.
9. Current `npm test` is intentionally not green: Node reported 292 tests, 282 pass, 10 fail, zero skip. Eight Bubble Pop T1-T4 phone/desktop subtests failed at the existing expected-red assertion and Node also counted their parent group as failed. The remaining reported failure was the whole `seek-companions.test.mjs` file under suite concurrency; an immediate isolated rerun passed all 6/6 in 16.25s, so this run supplies no reproduced companion product defect. The prior 290/290 result remains pre-red history, not current all-green evidence.

## Spelling repair at `33a26ea`

1. Pre-fix focused Learning resilience evidence reproduced all five Spelling T6-T10 phone rows as failures: 71 PASS / 12 FAIL / 23 NA / 15 BLK. T6-T9 failed geometry and visual-quality checks; T10 additionally failed input and wrong-answer recovery because GUITAR's required `U` rendered at `x=-64..-8` outside a 390px viewport with no horizontal scroll.
2. Red-first regression commit `f5f2ced` failed 8 of 15 bounded cases at the unchanged baseline. Product commit `33a26ea4851dc70d74d6bb8b51f2a577e5251de6` adds only width/box-sizing/padding constraints to the Spelling body and letter grid; words, scoring and difficulty logic are unchanged.
3. The permanent regression passed 15/15 after the fix. The focused T6-T10 Learning rerun passed all product checks: 77 PASS / 0 FAIL / 23 NA / 20 BLK across five rows. It proves no horizontal clipping, wrong-then-correct recovery and reload recovery for every repaired row; judgement-heavy and unrelated dimensions remain BLK.
4. Exact T6-T10 phone renders at 390x844 plus 320x568, tablet and desktop samples were inspected. Letter targets were 58-65px, the narrow phone used natural vertical scroll, and no horizontal overflow or unreachable tile remained. The independent exact-commit review found no P1/P2 issue at `33a26ea`; its expanded matrix passed 40/40 across T1-T10 and four viewports, with 316 letter clicks, 40 reloads, 3-8-letter words and duplicates.
5. Only seven canonical ledger dimensions changed from FAIL to PASS: `visual_quality` for T6-T10 phone plus `input` and `wrong_answers` for T10 phone. The five affected row verdicts changed from FAIL to BLK because other checks remain unfinished. Ledger totals are now **5,885 PASS / 16 FAIL / 8,933 BLK / 1,666 NA**; 16 rows are FAIL and 644 are BLK.
6. Current `npm test` is intentionally not green: 312 tests, 303 pass, 9 fail, zero skip. All failures are the existing expected-red Bubble Pop T1-T4 instruction assertions, including Node's parent-group accounting. `seek-companions.test.mjs` did not fail in this run; its earlier suite-concurrency-only failure remains separately recorded and is not reclassified as a product defect.
7. Animal Sounds remains report-only. T1-T4 returns from its garden branch before the quiz-only title, instruction and replay controls. All 30 manifest animals have a matching real sound file; seven additional sound files are present. The quiz cue `Which animal makes this sound?` is in the manifest at hash `37c85ff9` with all four configured voice clips, but it is the wrong instruction for exploration. A bounded later repair should show `Tap an animal to hear its sound!`, speak it once and expose replay in the garden without changing animals, sounds or scoring; that exact phrase is not in the manifest and would require four clips through the separately approved credential route. No secret, provider, paid or account call occurred.
8. Evidence images remain ignored and are not child-source commits. No production, remote, main, push, merge, deploy, promote, PII, security or release-gate action occurred.

## Art resilience at `2b500ff`

1. The accepted Art product baseline is Spelling product commit `33a26ea4851dc70d74d6bb8b51f2a577e5251de6`; Art source itself is unchanged from the prior accepted menu baseline. Runner checkpoints are `fb9e9b3`, corrected Color In drag probe `6dd3b49` and exact phone-target classification `2b500ff`. Synthetic profiles and a self-owned identity-checked localhost were used; no real family or provider state.
2. Final scope: all 4 Art routes × T1-T10 × desktop 1280x900 and touch-phone 390x844 = 80 rows. The activity-specific probes cover visible play affordance, real input/action recording, tiered tools or picture/scene progression where applicable, Clear/Undo or fresh-input recovery where applicable, rapid/multi-pointer input, boundary input, outside-drag rejection/recovery, unrelated keys, rotation with artwork preservation, mid-session reload, navigation during Stamp's little-kid animation, repeated exit/re-entry, malformed synthetic profile/upload state and controlled offline reload.
3. **Confirmed P2 child-experience defect — Color Splash phone color pips are 42x42px at every T1-T10 row, below the Kids 44px child-target floor.** All controls remain on-screen with zero horizontal overflow; desktop pips are 52px and pass. Each affected phone row has one internal layout failure and one canonical visual-quality failure. Reported in the Kids inbox before any fix; no Art product fix is authorized or made.
4. Final guarded report: **1,202 PASS / 20 expected FAIL / 628 explicit NA / 230 BLK** across 80 rows, with zero runner/runtime or unexpected product failures. Ten internal layout failures confirm the same 42px geometry and are not ledger dimensions; the importer records the ten matching canonical visual-quality failures only. A preliminary Color In desktop drag family was rejected as harness error because it reused an already-colored sample; the corrected known-ray probe passed all 10/10 desktop rows before the final full run.
5. Mechanic-level classifications stay explicit: score, wrong answers, media-file failure, HTML-media interruption and timer expiry are NA for these Art routes; progression is 32 PASS / 48 NA; reset/recovery and outside drag are each 60 PASS / 20 NA; only Stamp T1-T2 has an animation-interruption mechanic. Rewards, long soak and unaudited full visual judgement remain BLK rather than inferred from action events or geometry.
6. Guarded ledger import adds 972 verified PASS, 628 mechanic-level NA and ten canonical FAIL dimensions. Current canonical ledger: **6,857 PASS / 26 FAIL / 7,323 BLK / 2,294 NA** across 16,500 dimensions; 26 rows are FAIL and 634 are BLK. All 660 rows remain unfinished; PARTIAL/NOT_RUN stays distinct from a product failure or access/environment block.
7. Representative T1/T5/T10 phone and desktop renders for all four activities were retained ignored and sampled visually. The studios, navigation, canvases, docks, scene/page controls and age-expanded tools remain readable and unclipped apart from the documented 42px phone pips. Stamp T1's final offline evidence frame occurs before its delayed automatic demonstration; the runner separately waited for and verified the visible automatic first stamp, so the early blank capture is not classified as a missing cue.
8. No child screenshot is committed. Stale preliminary Color In failure captures remain ignored and are excluded from final evidence rather than deleted. No production/main, push, merge, deploy/promote, secrets, PII, provider/account/paid call, security, release-gate or remote-data action occurred.

## Color Splash repair at `9cffb54`

1. Permanent browser regression `01640c4` failed before the product change: all T1-T10 phone 390x844 cells and the T10 320x568 short-phone cell measured every visible color pip at 42x42px. T10 tablet 820x1180 and desktop 1280x900 passed at 52x52px. The red run reported 2 pass / 12 fail across 14 Node test results, including the failing parent group.
2. Product commit `9cffb545a9898502073449cd625a147eb7bce1c9` changes only the base `.vb-sw` width and height from 42px to 44px. The existing 768px media rule remains 52px. Palette count, layout, paint, progress recording and all activity logic are unchanged.
3. The fixed regression is 14/14. Its 13 device-tier cells cover T1-T10 phone plus 320 short phone, tablet and desktop; every palette target accepts input, no control clips or overlaps, horizontal overflow stays zero, ordinary/rapid/cancelled input remains responsive, Clear then fresh paint recovers, reload preserves the safe geometry and each finalized stroke records exactly once.
4. The full Art resilience rerun is 80/80 rows with 1,212 PASS / 0 FAIL / 628 NA / 240 BLK. All 20 Color Splash phone/desktop rows pass product checks. The complete project suite reports 317 pass / 9 expected Bubble Pop cue failures / 0 skip; it is not all green.
5. Independent exact-commit review in a separate detached checkout reproduced the 11 failing pre-fix device cells, then passed the focused 14/14 regression and full 80/80 Art matrix at `9cffb54`; no P1/P2 finding remained. Local phone renders at 390x844 and 320x568 were also inspected after real palette selection and painting with no clipping or overlap.
6. Only the ten canonical Color Splash T1-T10 phone `visual_quality` dimensions change from FAIL to PASS. Ledger totals become **6,867 PASS / 16 FAIL / 7,323 BLK / 2,294 NA**; 16 rows are FAIL and 644 remain BLK. All rows remain unfinished.

## Shared resilience at `955b701`

1. Selected the five stateful shared child paths with the highest remaining recovery risk: profile picker, child home, ribbon gallery, parent settings and Listening Hut. The already completed family add/delete, basic section-menu counts and activity-family resilience checks were not repeated. Video, About and Privacy remain the 60 wholly NOT_RUN shared rows.
2. Runner `955b70103a8d7b9fc7d283b772a737f7193c0784` used isolated synthetic profiles and a self-owned identity-checked localhost. Scope is five routes x T1-T10 x desktop 1280x900/touch-phone 390x844 = 100 rows. Final report: **1,160 PASS / 0 FAIL / 440 NA / 100 BLK** with no runtime or environment failure.
3. Every row exercised visible guidance, primary child input, rapid input, native keyboard activation, reload, orientation/viewport change, repeated child-home exit/re-entry, malformed optional state and controlled service-worker offline reload. Back/Home, empty values, rewards, timer and media dimensions were exercised only where the route actually owns them; inapplicable mechanics remain explicit NA. Full visual judgement stays BLK.
4. Profile selection preserved the exact selected child; a stale active profile recovered at the intact picker. Child-home reward navigation survived rapid activation and a missing active profile redirected safely. Parent settings unlocked through the real keypad, panel navigation survived repeated activation and null optional profile fields loaded with safe defaults. The gallery rendered one seeded earned ribbon and exact XP without duplication after malformed empty achievement fields.
5. All 20 Listening Hut rows set, rapidly changed, reloaded and cancelled wall-clock timers. A compressed positive timer expired through the real four-second fade, paused registered media exactly once, restored its original volume, cleared storage, showed the sleep veil and dismissed cleanly. This proves behavior under synthetic media; it does not claim that unavailable local music was heard.
6. Guarded import changes 1,020 BLK dimensions to PASS and 440 BLK dimensions to NA. Ledger totals become **7,887 PASS / 16 FAIL / 5,863 BLK / 2,734 NA** across 16,500 dimensions; row verdicts remain 16 FAIL / 644 BLK. All 660 rows remain unfinished; 600 are PARTIAL and 60 are NOT_RUN.

## Video, About and Privacy resilience at `61eb`

1. Ran all 60 previously NOT_RUN rows: Watch, About and Privacy at T1-T10 on desktop 1280x900 and touch phone 390x844. Every row used an isolated synthetic profile, a health-checked local server, a controlled installed-service-worker cache, external-request interception, reload, orientation/resize and repeated entry/exit.
2. Captured 60 real rendered screenshots and inspected all six ten-tier contact sheets plus full-size desktop representatives. Watch controls, parent guidance and channel tiles remain visible without clipping or horizontal overflow; About and Privacy remain readable from heading through footer on both viewports. These screenshots prove the rendered shell and documents, not the quality of remote media.
3. About and Privacy complete their locally applicable dimensions: native links, keyboard path from About to Privacy, reload, resize, repeated entry/exit, installed-app offline recovery and visual review. Game, scoring, media, timer and corrupt child-state dimensions are explicitly NA because the documents do not own those mechanics.
4. Watch passes local channel input, stubbed queue progression, 56x56px player Back geometry, keyboard operation, rapid input recovery, reload, orientation, repeated entry/exit, failed-feed messaging, malformed/hostile local state, installed-app offline failure messaging and twenty sequential player create/stop/destroy lifecycles. The lifecycle stub is not evidence that a real YouTube stream looks or sounds good.
5. **P2 — Watch pending-feed close lifecycle leak:** on every T1-T10 phone/desktop row, opening a channel and immediately pressing Back before the feed response completes hides the overlay, but `openChannel()` resumes after `await fetch` and constructs a new autoplay player. The hidden player receives no matching stop/destroy call because `closePlayer()` ran while `_player` was still null. The controlled run observed `created:1, destroyed:0, stopped:0, active:false` in every row. This proves the lifecycle leak and an ignored Back intent; it does not claim real audio was heard. Smallest proposed repair: cancellation generation/abort guard checked after feed and API awaits, plus a regression that closes during each pending phase. Product change needs separate scope and red-first authorization.
6. Existing accepted risk remains, not relabeled as a new finding: About and Privacy describe multi-device cloud sync, while current sign-in replaces the account's single sync key and invalidates the other device. `docs/CODEX-TRIAGE.md` item 12 already records this exact limitation as accepted risk pending Stage 1b architecture work.
7. Guarded ledger import changes 800 BLK dimensions to PASS, 20 to FAIL and 640 to NA. Totals become **8,687 PASS / 36 FAIL / 4,403 BLK / 3,374 NA**; row verdicts become 40 PASS / 36 FAIL / 584 BLK. Execution becomes 40 COMPLETE / 620 PARTIAL / 0 NOT_RUN. Watch stays PARTIAL because real remote stream endurance and audible interruption are genuinely blocked; About and Privacy are COMPLETE.

## Shared visual review and repeated-use soak at `61eb`

1. The ledger selected exactly two remaining locally testable dimensions on the five already-resilience-tested stateful routes: `visual_quality` and `long_repeated_play`. No accepted instruction, input, keyboard, reload, corruption, offline, timer-expiry or media check was repeated.
2. All 100 route/tier/viewport rows passed their focused soak: 20 profile-selection cycles, 20 child-home/gallery round trips, 20 gallery re-entries with exactly one earned ribbon, 100 settings-panel transitions with exactly one active panel, or 50 sleep-timer set/cancel cycles ending off with no stored timer, as appropriate.
3. Captured 100 real rendered screenshots across profile picker, child home, ribbons, unlocked parent settings and Listening Hut. All ten phone/desktop contact-sheet pairs were inspected. Headings, primary content, navigation and child controls remain visible and coherent; no horizontal overflow, overlap, clipped essential control or tier-specific visual break was found. The wide child world intentionally shows partial neighboring landmarks on phone as scroll affordance; essential huts, companion and ribbon access remain visible.
4. The guarded importer changes exactly 200 BLK dimensions to PASS. Ledger totals become **8,887 PASS / 36 FAIL / 4,203 BLK / 3,374 NA**; row verdicts remain 40 PASS / 36 FAIL / 584 BLK, and execution remains 40 COMPLETE / 620 PARTIAL. Other untested dimensions stay BLK; this batch does not claim the comprehensive audit is complete.
5. Final project suite: 326 tests, 317 pass / 9 fail / 0 skip. All nine failures are the existing Bubble Pop T1-T4 missing-cue assertions plus Node's parent-group accounting; the suite is not green. Both new guarded importers were run twice and changed zero dimensions on their second pass.
