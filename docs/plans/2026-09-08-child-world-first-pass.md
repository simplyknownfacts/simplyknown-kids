# Kids first-pass playable preview — September 8 (codex)

1. **Scope authorization:** Scott explicitly requested a full first-pass revamp to see before production, preserving the current promote gate and using focused subagents to reduce cost. This supersedes the previous inventory-only stopping point. The shared September 8 child-experience brief is the product direction. This is a local development preview, not a production approval or a claim that all release gates passed.
2. **Design decision:** a responsive illustrated island with separately drawn houses and paths replaces the flat panorama. Each house is its own button, so its hit area and feedback stay attached to its artwork. Portrait, landscape and desktop use deliberate compositions. The existing per-profile animated animal joins the scene. This keeps the product's existing profiles, activities, audio and offline storage instead of introducing another game/account system.
3. **Engineering decision:** retain the zero-build static HTML/CSS/JS runtime, existing routes and recorded audio, parent settings, sync interfaces, achievement ledger and publish allow-list. New house illustrations are original SVG rendered from fixed source strings; child data remains text. No new dependencies or vendor service calls in the product. Nonessential motion honors reduced motion. Small synthetic local preview entry is excluded from release staging and will not overwrite stored profiles.
4. **Independent work:** root owns home/art/companion and integration; separate worktrees own section/shared UI, speech/rewards/Count/Shape resilience, and activity content/inventory. Integrate only reviewed commits. Every checkout has one writer. GPT-5.6 Sol/high handles bounded subagent work; root integrates and verifies. No background schedules or additional vendor generation costs.
5. **Verification plan:** browser-test real house geometry/taps at 390×844, 320×568, tablet, desktop and phone landscape; verify profile animal selection, movement and reduced-motion fallback. Reproduce repeated-answer/speech interruption paths and prove single completion, single reward and safe navigation. Run the required full node suite and browser drive against the exact local preview, inspect screenshots and errors, then independently review a fixed commit. Physical-device behavior is reported separately from emulation.
6. **Release preservation:** keep `scripts/promote.mjs`, dev verification/stamping, staging allow-list, release log and production workflows intact. Verify gate tests and inspect the gate-file diff before delivery. No fake DEV-VERIFIED stamp, main merge/push, production deploy or remote configuration change. Existing GitHub Pages bypass/cutover issue remains documented. Scott sees this version before any production proposal.
7. **First-pass limits:** content inventory must distinguish source review, runtime checks and untested modes. Original existing mascot clips vary in visual style; this pass integrates them, with future art-direction consistency recorded as debt. A visual preview is not evidence that all ages/modes or Scott's physical-device freeze are solved.
